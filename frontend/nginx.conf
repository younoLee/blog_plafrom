server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA 라우팅: /blog, /status 같은 경로를 새로고침해도
    # 실제 파일이 없으면 index.html을 돌려줘서 React Router가 처리하게 함
    location / {
        try_files $uri $uri/ /index.html;
    }
}
