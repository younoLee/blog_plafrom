# starlette는 fastapi가 끌어오지만 재현성을 위해 명시 고정한다.
# (옛 주석은 0.115.x 시절 CVE-2024-47874 대응 근거였는데, 2026-07-22에 fastapi 0.139 /
#  starlette 1.3 대로 올리면서 그 버전대 설명이 무의미해져 정리했다.)
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.52.1
sqlalchemy==2.0.51
alembic==1.19.1
pydantic-settings==2.15.0
email-validator==2.3.0
python-multipart==0.0.32  # CVE-2024-53981: multipart 경계 파싱 DoS 수정 버전
bcrypt==5.0.0
PyJWT==2.13.0
psycopg2-binary==2.9.12
python-dotenv==1.2.2
anthropic==0.121.0
slowapi==0.1.10
boto3==1.43.67
cryptography==50.0.0
openai==2.53.0
google-genai==2.17.0
cohere==7.0.8
psutil==7.2.2  # 관리자 인프라 대시보드 실측(CPU·메모리·디스크·부하)

# Web Push (VAPID + aes128gcm). pywebpush 대신 그것이 내부에서 쓰는 두 라이브러리만
# 직접 넣는다 — 암호화·서명이라는 '틀리면 조용히 깨지는' 부분은 같은 코드가 처리하고,
# 우리가 쓰는 건 헤더 조립과 POST뿐이라 검토가 가능하다. 대가로 의존성이 11개→2개다
# (pywebpush는 aiohttp 생태계를 통째로 끌고 온다). HTTP는 아래 httpx로 보낸다.
http-ece==1.2.1
py-vapid==1.9.4
# 지금까지 anthropic/openai가 끌어오던 것을 직접 쓰게 됐으므로 명시 고정한다.
# 전이 의존성에 얹으면 그쪽 SDK가 빠지거나 범위를 바꿀 때 조용히 사라진다.
httpx==0.28.1
