# 테스트 전용 의존성 (프로드 이미지엔 안 들어감). 설치: pip install -r requirements-dev.txt
pytest==8.3.4
httpx==0.27.2  # FastAPI TestClient가 내부적으로 사용
pytest-cov==5.0.0  # 커버리지 측정 (pytest.ini에서 app 대상 리포트)
ruff==0.8.4  # 린터 (설정: ruff.toml). 안 쓰는 import·잠재 버그·import 정렬 검사
