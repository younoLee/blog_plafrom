[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = .
# 커버리지: app 패키지 대상, 미커버 라인 표시. 백그라운드 스레드/외부 SDK 경로는
# 통합테스트로 못 닿는 게 있어 100%는 목표 아님 — 회귀로 '떨어지는' 걸 막는 게 목적.
addopts = -q --cov=app --cov-report=term-missing --cov-fail-under=70
