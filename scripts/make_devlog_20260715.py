"""2026-07-15 개발일지(고정 IP + 구독 UI 통합 + 토스페이먼츠 Pro 결제 + 댓글 버그)를 Word로.
   EIP로 '켤 때마다 주소 바뀜' 해결, 구독을 한 카드로, 실제 결제 붙이기(서버 검증이 핵심),
   그리고 '글은 보이는데 댓글만 안 뜨는' 버그. WSL + Windows host 양쪽 저장."""

import os
import shutil

from docx import Document
from docx.shared import Pt, RGBColor
from docx.oxml.ns import qn

DATE = "2026-07-15"
FILENAME = f"블로그_개발일지_{DATE}.docx"
PROJECT_PATH = f"/home/es0764/blog_plafrom/{FILENAME}"

doc = Document()
normal = doc.styles["Normal"]
normal.font.name = "맑은 고딕"
normal.font.size = Pt(10.5)
normal._element.rPr.rFonts.set(qn("w:eastAsia"), "맑은 고딕")

BLUE = RGBColor(0x1F, 0x6F, 0xEB)
RED = RGBColor(0xC0, 0x39, 0x2B)
GRAY = RGBColor(0x55, 0x55, 0x55)
GREEN = RGBColor(0x1E, 0x7E, 0x34)


def h1(t):
    doc.add_heading(t, level=1)


def p(t, bold=False):
    para = doc.add_paragraph()
    r = para.add_run(t)
    r.bold = bold
    return para


def bullet(t):
    doc.add_paragraph(t, style="List Bullet")


def analogy(t):
    para = doc.add_paragraph()
    r = para.add_run("🔎 비유   ")
    r.bold = True
    r.font.color.rgb = BLUE
    r2 = para.add_run(t)
    r2.font.color.rgb = BLUE
    para.paragraph_format.left_indent = Pt(12)


def field(label, text, color=GRAY):
    para = doc.add_paragraph()
    para.paragraph_format.left_indent = Pt(12)
    r = para.add_run(f"{label}  ")
    r.bold = True
    r.font.color.rgb = color
    para.add_run(text)
    return para


def note(t):
    para = doc.add_paragraph()
    r = para.add_run("🛠 전문가 노트   ")
    r.bold = True
    r.font.color.rgb = GREEN
    r2 = para.add_run(t)
    r2.font.color.rgb = GREEN
    para.paragraph_format.left_indent = Pt(12)


# ===================== 표지 =====================
doc.add_heading("블로그 개발일지", level=0)
p(f"날짜: {DATE}", bold=True)
p("주제: 서버 주소를 '고정 IP'로 못 박기 + 구독 화면을 한 카드로 정리 + 진짜 결제(토스페이먼츠) 붙이기, "
  "그리고 '글은 보이는데 댓글만 안 뜨는' 버그 잡기")
p("대상: 웹/클라우드 입문자가 읽어도 이해되게 — 고정 주소가 왜 필요한지, 결제는 왜 서버가 확인해야 하는지, "
  "권한 버그가 어떻게 '데이터 없음'처럼 위장되는지 위주로.")
doc.add_paragraph("")

# ===================== 0. 한눈에 =====================
h1("0. 오늘 한 일 한눈에")
p("지난번에 남긴 숙제('서버를 껐다 켜면 주소가 바뀌어서 CDN 설정을 매번 고쳐야 함')를 먼저 없앴다. 그 다음 구독 화면을 "
  "정리하고, 그 위에 실제 돈을 받는 결제를 붙였다. 마지막으로 사용자가 겪을 뻔한 댓글 버그를 잡았다.")
bullet("고정 IP(EIP): 서버를 껐다 켜도 주소가 안 바뀌게 → 이제 '끄고 켜기'가 부담 없음(= 비용절감 자유롭게)")
bullet("BYOK 키 검증: 사용자가 넣는 AI API 키가 '엉뚱한 값'이면 저장 전에 거절")
bullet("구독 UI 통합: 흩어진 두 섹션을 '구독' 한 카드로. 구독한 사람만 새 글 알림 설정 가능")
bullet("토스페이먼츠 Pro 결제: 결제창 → 서버가 토스에 직접 확인 → 상위 AI 모델 해금(30일)")
bullet("버그 수정: 비공개·구독자공개 글에서 '글은 보이는데 댓글만 안 뜨던' 문제")
analogy("가게로 치면 — 가게 주소를 임시번호에서 '고정 번지수'로 바꾸고(EIP), 회원가입 창구를 하나로 합치고, "
        "카드결제기를 들이고(단 영수증은 카드사에 직접 확인), 마지막으로 'VIP룸 손님인데 방명록만 안 보이던' 오류를 고친 날.")

# ===================== 1. 고정 IP =====================
h1("1. 고정 IP(EIP) — 껐다 켜도 주소가 그대로")
p("지난 세션의 숙제였다. 비용을 아끼려 서버(EC2)를 정지했다가 다시 켜면 퍼블릭 IP가 새로 배정됐다. 그러면 CDN(CloudFront)이 "
  "바라보는 '원본 서버 주소'가 옛날 주소라 사이트가 깨진다 → 켤 때마다 수동으로 고쳐야 했다.")
field("고친 방법", "AWS의 탄력적 IP(EIP)를 서버에 붙였다. EIP는 '내가 소유한 고정 주소'라 서버를 껐다 켜도 그대로다. "
                 "그리고 CDN의 원본 주소를 하드코딩 대신 '그 EIP의 DNS 이름을 참조'하게 바꿔서, 앞으로는 손댈 일이 없다.", GREEN)
analogy("매번 바뀌는 임시 전화번호를 쓰다가, '내 명의의 고정 번호'를 받은 것. 가게 문을 닫았다 열어도 번호가 그대로라 "
        "전단지(CDN 설정)를 다시 찍을 필요가 없다.")
note("인프라 설정에서 주소를 하드코딩하지 말고 '리소스 참조'로 쓰면, 값이 바뀌어도 코드가 알아서 따라간다 — "
     "이게 IaC(코드로 인프라 관리)의 핵심 이득이다.")
field("[2026-07-17 추가] 이 결정은 이틀 뒤 되돌렸다", "① 요금: 2024년 2월부터 '모든 퍼블릭 IPv4'가 시간당 $0.005다. "
      "즉 EIP를 써도 안 써도 켜져 있는 동안 값은 같고(프리티어 12개월은 월 750시간 무료라 지금은 $0), EIP는 "
      "'정지된 인스턴스에 붙어 있으면' 과금된다. ② 그래서 EIP를 뺐는데, 빼는 과정에서 더 중요한 걸 발견했다 — "
      "IP를 놓으면 그 주소는 남에게 배정되고, 오리진에 남은 옛 호스트명이 그 남의 서버를 가리키게 된다(dangling origin). "
      "자세한 건 2026-07-17 일지 참고.", RED)
p("결과: 오늘부터 '안 쓸 땐 EC2·RDS 정지'가 아무 마찰 없이 가능해졌다. 지난 일지의 ⚠️ 경고(재시작 시 오리진 갱신)는 해소됐다.")

# ===================== 2. BYOK 키 검증 =====================
h1("2. AI 키(BYOK) 형식 검증 — 엉뚱한 값은 저장 전에 거절")
p("이 블로그는 사용자가 자기 AI API 키를 넣어 쓰는 기능(BYOK, Bring Your Own Key)이 있다. 그런데 여태 '무엇을 넣든' "
  "암호화해서 저장했다. 오타나 통째 붙여넣기 사고가 나도 그대로 들어갔다.")
field("고친 방법", "저장 전에 형식을 검사해 실패하면 400으로 거절한다. ① 공통: 공백·탭·개행이 섞였거나 제어문자·비ASCII가 "
                 "있으면 거절(설명문을 통째로 붙여넣는 사고 방지). ② 업체별 접두사: openai는 sk-, anthropic은 sk-ant-, "
                 "gemini는 AIza로 시작해야 한다.", GREEN)
field("일부러 안 한 것", "compatible(호환 서버)·cohere는 업체마다 키 형식이 제각각이라 접두사를 강제하지 않고 공통 검증만 "
                    "했다. 규칙이 불안정한 곳에 억지 규칙을 넣으면 멀쩡한 키를 막는다.", GRAY)
analogy("금고에 넣기 전에 '이게 진짜 열쇠 모양인지'만 확인하는 것. 그 열쇠가 실제로 문을 여는지는 써봐야 알지만, "
        "적어도 '열쇠가 아닌 물건'은 금고에 안 들어간다.")
note("검증은 '확실한 것만' 하는 게 좋다. 공개적으로 안정적인 규칙(접두사)만 강제하고, 애매한 건 통과시켜야 오탐이 안 난다. "
     "그리고 검증은 암호화 저장보다 '앞'에 둬야 의미가 있다 — 잘못된 값이 금고까지 들어간 뒤엔 확인이 더 비싸진다.")

# ===================== 3. 구독 UI =====================
h1("3. 구독 화면을 한 카드로 — 순서가 있는 일은 순서대로 보이게")
p("'구독 관리' 화면에 섹션이 두 개로 흩어져 있었다: 이메일 뉴스레터 구독과, 글쓴이별 새 글 알림. 사용자는 둘의 관계를 "
  "알 수 없었다(알림을 켜려면 먼저 구독이 돼 있어야 하는데 그게 안 보였다).")
field("고친 방법", "로그인 사용자 전용 '구독' 카드 하나로 합쳤다. 위엔 '내 계정 이메일로 원클릭 구독', 아래엔 글쓴이별 "
                 "알림 토글. 구독 전에는 알림 토글이 잠겨(비활성) 보인다 → 할 일의 순서가 화면에 드러난다.", GREEN)
field("중요한 정책 판단", "기존 익명 구독은 '더블옵트인'(확인 메일의 링크를 눌러야 구독 확정)이다. 남의 이메일을 마음대로 "
                    "구독시키는 걸 막는 방어선이다. 그런데 로그인 사용자가 '자기 계정 이메일'을 구독하는 건 이미 "
                    "로그인으로 소유가 증명된 상태 → 확인 메일 없이 즉시 확정해도 안전하다. 익명 경로의 더블옵트인은 "
                    "그대로 뒀다.", BLUE)
analogy("회원카드를 이미 보여준 사람에게 '본인 확인 문자'를 또 보내지 않는 것. 단, 길에서 이름만 적어낸 사람에겐 "
        "여전히 확인 문자를 보낸다.")
note("보안 정책은 '일괄 적용'이 아니라 '증명 수준에 따라' 정하는 것이다. 같은 행동(구독)이라도 신원이 증명된 경로와 "
     "익명 경로는 요구사항이 다르다. 단, 완화는 반드시 '증명이 이미 됐는가'를 근거로만 — 편의를 근거로 하면 구멍이 된다.")
field("겪은 함정", "FastAPI는 라우트를 '선언한 순서'로 매칭한다. /subscribers/me를 /subscribers/{id}보다 뒤에 두면 "
                "'me'를 숫자 id로 해석하려다 깨진다. /me를 앞으로 옮겨 해결.", RED)

# ===================== 4. 결제 =====================
h1("4. 토스페이먼츠 Pro 결제 — 오늘의 핵심")
p("Pro 구독(월 9,900원)을 결제하면 상위 AI 모델이 30일간 열린다. 결제는 처음이라 '어디를 믿으면 안 되는가'를 제일 신경 썼다.")
field("흐름", "① 서버가 주문을 만든다(/checkout) → ② 사용자가 토스 결제창에서 결제 → ③ 성공 페이지로 돌아옴 → "
             "④ 서버가 토스 승인 API에 직접 물어 검증(/confirm) → ⑤ 통과해야 Pro가 켜지고 만료일이 정해진다.", GREEN)
p("여기서 절대 하면 안 되는 것: ③에서 '성공했대요'라는 브라우저의 말만 믿고 Pro를 켜는 것. 브라우저에서 오는 값은 "
  "사용자가 마음대로 바꿀 수 있다(금액을 100원으로, 남의 주문번호로). 그래서 ④에서 서버가 토스에 직접 확인한다.")
analogy("손님이 '카드 됐어요'라고 말한다고 물건을 내주지 않는다. 가게 단말기가 카드사에 직접 물어 '승인 확인'이 떠야 내준다. "
        "손님이 보여주는 화면 캡처는 증거가 아니다.")
field("붙인 안전장치", "① 금액 위변조 방지: 클라이언트가 보낸 금액이 서버가 만든 주문 금액과 다르면 거절. "
                   "② 남의 주문 차단: 주문 소유자만 승인 가능. ③ 멱등 처리: 같은 결제를 두 번 confirm해도 한 번만 반영. "
                   "④ PAYMENTS_REQUIRE_LIVE=true면 테스트 키(test_)로는 승인 거부 → '테스트 키로 공짜 Pro' 사고 차단.", GREEN)
field("키 관리", "시크릿 키는 서버 전용(.env)이라 절대 브라우저로 안 나간다. 클라이언트 키는 원래 공개되는 값이라 "
               "빌드 시 주입(VITE_TOSS_CLIENT_KEY). 기본값은 토스 공개 테스트 키라 지금은 실제 청구가 없다.", GRAY)
note("결제에서 '클라이언트 키가 공개'인 건 사고가 아니라 설계다. 공개 키는 '결제창을 띄우는' 권한만 있고, '승인'은 "
     "시크릿 키를 가진 서버만 할 수 있다. 이 비대칭이 결제 보안의 뼈대다.")
field("만료 처리", "Pro는 pro_until(결제일 + 30일)이 지나면 자동으로 꺼진다. 매일 도는 배치를 만들지 않고, "
                "사용자가 요청할 때 '만료됐으면 그때 끈다'(lazy expiration). 서버가 하나뿐인 프로젝트에선 이게 단순하고 충분하다.", GRAY)
note("배치(주기 작업)는 인프라와 실패 처리를 늘린다. '값을 읽는 순간에 판정'하면 코드 한 줄로 같은 효과를 낸다 — "
     "단, 만료된 상태가 '읽히기 전까지' DB에 남아 있으니, 통계를 낼 땐 그 점을 감안해야 한다.")

# ===================== 5. 댓글 버그 =====================
h1("5. 버그 — '글은 보이는데 댓글만 안 뜬다'")
p("비공개 글과 구독자공개 글에서 본문은 잘 보이는데 댓글만 안 떴다. 증상이 묘해서 원인이 헷갈리는 종류의 버그였다.")
field("원인", "프론트가 댓글을 가져올 때 로그인 토큰(Authorization 헤더)을 안 보냈다. 백엔드는 댓글에도 글 열람권한을 "
             "확인하는데, 토큰이 없으니 '익명'으로 보고 비공개 글에 404(없음)를 줬다. 반면 글 본문 요청은 토큰을 보내고 "
             "있어서 잘 떴다 → '글은 보이는데 댓글만 안 뜨는' 조합이 된 것.", RED)
field("고친 방법", "댓글 조회·작성 두 호출에 인증 헤더를 추가. 이제 소유자·구독자·관리자가 정상적으로 읽고 쓴다. "
                 "(로그인 사용자의 댓글 작성자명은 백엔드가 계정명으로 고정해 사칭을 막는다.)", GREEN)
analogy("VIP룸에 입장은 했는데(본문), 방명록을 달라고 할 때만 회원증을 안 보여준 것. 직원은 '그런 방명록 없습니다'(404)라고 "
        "답했고, 손님은 '왜 나만 방명록이 안 보이지?' 하고 헤맸다.")
note("이 버그의 교훈은 '404의 위장'이다. 권한 검사는 보통 '없는 척'(404)으로 응답한다 — 존재 여부를 숨기는 게 보안상 "
     "맞기 때문이다. 그래서 권한 버그가 '데이터가 없다'처럼 보인다. 프론트에서 '어떤 요청에 인증을 붙였고 안 붙였는지'를 "
     "일관되게 관리해야 이런 걸 안 만든다.")

# ===================== 6. 검증/비용 =====================
h1("6. 오늘의 검증과 비용")
bullet("인프라: terraform apply로 EIP·CDN 오리진·CSP(토스 결제창 허용) 반영")
bullet("프론트: GitHub Actions 자동배포 success(마지막 커밋까지)")
bullet("백엔드: /api/status·/api/posts 정상, 결제·구독 엔드포인트는 무인증 접근 차단 확인")
bullet("DB: 마이그레이션 2개(payments 테이블, users.pro_until) 적용")
p("끝내면서 EC2·RDS를 정지해 비용을 멈췄다. 지난번과 달리 EIP 덕분에 '다시 켤 때 오리진 갱신'은 이제 필요 없다.")

# ===================== 7. 다음 =====================
h1("7. 다음 할 일")
bullet("(선택) 진짜 글 콘텐츠 채우기 — 하이라이팅·태그·커버가 다 준비됐으니 이제 내용만")
bullet("(실결제 전환 시) PAYMENTS_REQUIRE_LIVE=true + 토스 라이브 키로 교체")
bullet("(선택) 비용: RDS를 EC2 안 Postgres로 이전(-$21/월) / 예산 알림(AWS Budgets)")
bullet("(선택) 커스텀 도메인 + 오리진 HTTPS, CloudWatch 상세 지표(유료)")

# ===================== 저장 =====================
doc.save(PROJECT_PATH)
print(f"[저장] 프로젝트(WSL): {PROJECT_PATH}")

host_user = "/mnt/c/Users/USER"
cands = [
    os.path.join(host_user, "Desktop"),
    os.path.join(host_user, "OneDrive", "Desktop"),
    os.path.join(host_user, "바탕 화면"),
    os.path.join(host_user, "Documents"),
    host_user,
]
host_dir = next((d for d in cands if os.path.isdir(d)), host_user)
host_path = os.path.join(host_dir, FILENAME)
shutil.copy2(PROJECT_PATH, host_path)
print(f"[저장] Windows host: {host_path}")
