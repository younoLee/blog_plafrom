#!/usr/bin/env python3
"""개발일지 #32 (2026-08-15) — 처방이 이 집에서만 안 들었다.

서식은 scripts/devlog_doc.py가 맡는다. 이 파일엔 내용만 있다.
실행 (WSL 파이썬에 python-docx가 없어 컨테이너로):

    docker run --rm -v "$PWD":/w -v "/mnt/c/Users/USER/Desktop/개발일지":/desktop \\
      -w /w python:3.12-slim sh -c "pip install -q python-docx && python scripts/make_devlog_20260815.py"
"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from devlog_doc import DevlogDoc  # noqa: E402

d = DevlogDoc("2026-08-15", "블로그 만들기 #32")

d.cover(
    subject="처방이 이 집에서만 안 들었다",
    audience="입문자가 읽어도 이해되게 — 인터넷에서 찾은 정답이 왜 내 프로젝트에서는 안 돌 수 있는지,"
    " 그리고 '안 도는데 멀쩡해 보이는' 고장을 어떻게 잡는지 위주로.",
    note="앞 편들처럼 실제 터미널 출력을 그대로 실었다. 회색 고정폭 글씨는 전부 그날 화면에"
    " 찍힌 문자열이다. 내가 틀린 자리도 틀린 채로 남겼다.",
)

# ── 1 ────────────────────────────────────────────────────────────────────────
d.h1("1. 남은 목록을 다 닫기로 했다")

d.p(
    "어제 검사에서 '이 블로그에 부족한 것' 20건을 뽑아 순위를 매겼고, 그중 9건만 처리한 채 끝냈다. "
    "오늘은 **남은 11건을 전부** 닫기로 하고 앉았다."
)
d.p(
    "결론부터 적으면 다 닫았다. 그런데 이 편의 이야기는 '11건을 처리했다'가 아니다. "
    "그 11건 중 **세 건에서 똑같은 일이 벌어졌다** — 웹에서 정석이라고 하는 처방을 그대로 붙였는데, "
    "이 프로젝트에서만 **조용히 안 돌았다.**"
)
d.p(
    "셋 다 공통점이 있었다. 안 돌아도 **화면은 멀쩡해 보인다.** 에러도 없고, 로그도 없고, "
    "테스트도 초록이다. 그래서 이 편은 그 세 번의 이야기다."
)

# ── 2 ────────────────────────────────────────────────────────────────────────
d.h1("2. 첫 번째 — 폰트 한 줄이 첫 화면을 인질로 잡고 있었다")

d.p(
    "목록 10위는 '폰트 CSS가 남의 CDN에서 블로킹 로드된다'였다. 브라우저는 스타일시트를 다 받기 "
    "전까지 화면을 그리지 않는다. 그 스타일시트가 우리 서버가 아니라 **제3자(jsdelivr)** 에 있다."
)
d.p(
    "이 사이트는 'EC2가 꺼져 있어도 글은 읽힌다'를 설계 원칙으로 삼는다. 정적 아카이브를 따로 만든 "
    "이유가 그것이다. 그런데 정작 **첫 화면이 남의 집에 매여 있었다.** 그쪽이 느려지면 우리 S3와 "
    "CloudFront가 아무리 멀쩡해도 방문자는 흰 화면을 본다."
)
d.p("처방은 웹에 널려 있다. 한 줄이다.")
d.ev(
    "널리 쓰이는 처방",
    [
        '<link rel="stylesheet" media="print" onload="this.media=\'all\'" href="...">',
        "",
        "# media=print → 브라우저가 '지금 화면엔 안 쓰는 스타일'로 보고 받되 막지 않는다",
        "# 다 받으면 onload가 media를 all로 되돌려 그때 적용시킨다",
    ],
)
d.p(
    "이 저장소에서는 **절대 안 돈다.** CSP(콘텐츠 보안 정책)를 `script-src 'self'`로 잠가놨기 "
    "때문이다. 자기 출처의 스크립트 파일만 실행하고, HTML 속성에 박힌 인라인 코드는 차단한다. "
    "`onload=\"...\"` 가 정확히 그 인라인 코드다."
)
d.warn(
    "차단되면 media가 print에 영원히 머문다. CSS는 받았는데 폰트는 안 쓰는 상태가 된다. "
    "그런데 화면은 시스템 기본 글꼴로 폴백해서 **그럭저럭 보인다.** "
    "즉 '고쳤다'고 기록한 채 넘어가기 딱 좋은 모양이다."
)
d.p(
    "그래서 되돌리는 코드를 **자기 출처 모듈**로 옮겼다. `src/fontSwap.ts` 파일 하나를 만들고 "
    "앱이 뜰 때 부른다. CSP는 이건 막지 않는다."
)
d.analogy(
    "택배 상자에 '도착하면 열어주세요'라고 **매직으로 적어두는 것**과, 문 앞에 사람을 "
    "세워두는 것의 차이다. 경비실 규정상 상자에 적힌 지시는 무시하게 되어 있으면, "
    "아무리 크게 적어도 상자는 안 열린다."
)
d.p(
    "그리고 이 짝이 다시 깨지지 않게 **두 파일을 함께 읽는 테스트**를 붙였다. HTML에 `media=\"print\"`가 "
    "있는지, 그리고 그걸 되돌리는 호출이 실제로 코드에 있는지를 소스 텍스트로 검사한다. "
    "한쪽만 고치면 빨간불이 난다."
)

# ── 3 ────────────────────────────────────────────────────────────────────────
d.h1("3. 두 번째 — 같은 함정을 그날 안에 또 밟았다")

d.p(
    "목록 15·16위는 '아카이브에 검색과 태그 필터가 없다'였다. 31편이 날짜순으로 한 줄씩 쌓여 있어서, "
    "\"보안 얘기가 어느 편이었지\"를 찾으려면 제목 31개를 눈으로 훑는 수밖에 없었다."
)
d.p(
    "검색·필터는 자바스크립트가 필요하다. 정적 HTML 파일에 자바스크립트를 넣는 가장 흔한 방법은 "
    "페이지 안에 `<script>` 블록을 쓰는 것이다. 그렇게 짰다."
)
d.p(
    "그리고 **바로 그 순간 위와 똑같은 벽에 부딪힌다는 걸 알아챘다.** 인라인 스크립트도 "
    "`script-src 'self'`가 막는다. 막히면? 목록 31편은 그대로 보이고 **필터만 조용히 안 뜬다.**"
)
d.note(
    "아침에 폰트에서 밟은 함정을 오후에 다른 얼굴로 다시 만난 셈이다. "
    "다만 이번엔 붙이기 전에 알아챘다 — 같은 날 두 번째라서다."
)
d.p("그래서 필터 스크립트를 별도 파일로 뽑아 서빙한다. 그리고 짜는 방향도 뒤집었다.")
d.bullet("목록 31편은 **HTML에 전부 들어 있다** — 크롤러도, 자바스크립트가 막힌 브라우저도 다 본다")
d.bullet("필터 UI만 `hidden`으로 숨겨뒀다가 **스크립트가 성공하면 그때 드러낸다**")
d.p(
    "반대로 짰다면 — 필터가 목록을 그리는 구조였다면 — 스크립트가 막히는 순간 이 페이지가 **백지**가 "
    "된다. 그건 '서버도 스크립트도 없이 읽힌다'는 이 페이지의 존재 이유를 정면으로 부순다."
)
d.p(
    "덤으로 편마다 태그 칩과 '비슷한 주제의 편'을 붙였다. 관련도를 셀 때 **'개발일지' 태그는 뺐다.** "
    "31편 전부에 붙어 있어서, 그대로 두면 아무 두 편이나 1점씩 겹쳐 '관련 글'이 사실상 "
    "최신 3편 고정이 된다. 추천처럼 보이지만 정보가 0인 물건이다."
)

# ── 4 ────────────────────────────────────────────────────────────────────────
d.h1("4. 세 번째 — 어제 고친 버그가 새 얼굴로 돌아왔다")

d.p(
    "목록 11위는 '댓글 알림이 없다'였다. 이 블로그는 익명 댓글이 열려 있는데, 누가 댓글을 달면 "
    "글쓴이가 그 글에 **다시 들어가 눈으로 보기 전까지** 모른다. 종은 있는데 이 사건만 종을 안 울렸다."
)
d.p("만드는 것 자체는 어렵지 않았다. 알림 한 줄을 만들고, 글쓴이 기기로 푸시를 보낸다.")
d.p(
    "그런데 붙이고 나서 서비스워커(브라우저에서 알림을 띄우는 코드)를 다시 읽다가 한 줄에 걸렸다."
)
d.ev(
    "frontend/public/sw.js — 어제까지의 코드",
    [
        "tag: 'new-post',        // 같은 tag의 알림은 덮어쓴다",
        "renotify: true,         // 어제 고친 것",
    ],
)
d.p(
    "`tag`는 알림을 묶는 이름표다. 같은 이름표끼리는 **덮어쓴다.** 글을 연달아 올려도 알림이 "
    "쌓이지 않게 하려고 넣은 것이다. 어제(#31) 이것 때문에 하루를 썼다 — 이름표만 있고 "
    "`renotify`가 없어서, 두 번째 알림부터 소리도 배너도 없이 앞 알림을 갈아치우고 있었다."
)
d.p(
    "그 이름표가 **`'new-post'` 한 값으로 고정**이다. 그러면 오늘 만든 댓글 알림도 같은 이름표를 "
    "달게 되고, **댓글 알림이 새 글 알림을 지운다.** 어제 고친 그 버그의 다른 얼굴이다.",
    bold=True,
)
d.analogy(
    "우편함에 '광고' 딱지 하나만 붙여놓고 그 칸에 뭐든 넣는 것과 같다. "
    "전기요금 고지서를 넣으면 앞에 있던 통신비 고지서가 사라진다. "
    "둘 다 정상적으로 배달됐는데, 받는 사람은 하나만 본다."
)
d.p(
    "고침은 이름표를 서버가 정하게 하는 것이다. 새 글은 `new-post`, 댓글은 `comment-<글번호>`. "
    "글 번호를 넣어야 다른 글의 댓글끼리도 안 겹친다."
)
d.p(
    "댓글 **본문은 알림에 넣지 않았다.** 익명 입력이라 잠금화면에 그대로 띄우면 "
    "아무나 남의 잠금화면에 글자를 쓸 수 있게 된다. 누가 어느 글에 달았는지만 알린다."
)

# ── 5 ────────────────────────────────────────────────────────────────────────
d.h1("5. 그런데 알림이 데려다주지 않았다")

d.p(
    "댓글 알림을 누르면 `/blog/posts/48#comments`로 간다. 주소 끝의 `#comments`는 "
    "'그 페이지의 댓글 자리로 스크롤하라'는 뜻이다."
)
d.p("확인해보니 **이 앱에는 그 처리가 아예 없었다.** 이유가 둘이다.")
d.bullet(
    "브라우저의 기본 스크롤은 **문서를 다 받은 시점**에 일어난다. 그런데 이 화면은 글 내용을 "
    "그다음에 서버에서 받아온다 — 그 시점엔 `#comments`도 소제목도 아직 화면에 없다"
)
d.bullet("앱 안에서의 이동은 라우터가 처리하므로 브라우저 스크롤이 애초에 안 걸린다")
d.p(
    "그래서 알림을 눌러도 글 맨 위에 떨어졌다. 긴 글이면 뭐가 달렸는지 보려고 끝까지 내려야 한다. "
    "**곁들여 하나 더 드러났다** — 목차의 소제목 링크도 새로고침하면 안 먹고 있었다. 같은 원인이다.",
    bold=True,
)
d.p("본문이 붙은 뒤 한 번 맞추도록 열 줄쯤을 넣었다. 알림도, 목차도 같이 고쳐졌다.")

# ── 6 ────────────────────────────────────────────────────────────────────────
d.h1("6. 요금제가 계획을 거절했다")

d.p(
    "목록 20위는 '조회수·방문자 지표가 없다'였다. 31편 24만 자를 써놓고 **읽히는지 아닌지 아는 "
    "방법이 하나도 없었다.**"
)
d.p(
    "앱에 카운터를 다는 건 답이 아니다. 이 사이트의 읽기는 대부분 서버를 안 거친다 — EC2는 평소 "
    "꺼져 있고, 검색·RSS·공유로 들어오는 사람은 S3의 정적 아카이브를 본다. 백엔드에서 세면 "
    "**가장 많이 읽히는 경로가 통째로 안 세진다.** 모든 요청이 지나가는 지점은 CloudFront뿐이다."
)
d.p("그래서 CloudFront 액세스 로그를 S3로 쌓는 설계를 짜고, 인프라 코드까지 다 썼다. 적용했더니:")
d.ev(
    "terraform apply",
    [
        "Error: updating CloudFront Distribution (E1438IL9CSVBS4):",
        "  InvalidArgument: Distributions with the Free pricing plan",
        "  can't have the following features: Standard logging",
    ],
)
d.p(
    "요금제가 그 기능을 안 준다. 사실 이 제약은 처음이 아니다 — 보안 헤더(CSP)도 같은 이유로 "
    "정석 방법이 막혀서 우회로를 썼었다. 다만 그때는 우회로가 있었고, **로깅은 없다.**"
)
d.p("그래서 방향을 틀었다. CloudWatch 지표는 같은 요금제에서도 무료로 나온다. 실제로 값이 있었다.")
d.ev(
    "scripts/traffic_report.sh — 최근 7일",
    [
        "총 요청 1542건 / 6일 (하루 평균 257건)",
        "",
        "2026-08-11     245  ########",
        "2026-08-13      91  ###",
        "2026-08-14    1095  ########################################",
    ],
)
d.p(
    "대가가 있다. **어느 글이 읽혔는지는 못 본다.** 요청 단위 로그가 있어야 알 수 있고, 그건 돈을 "
    "더 내야 하는 결정이라 지금 하지 않는다. 그 한계를 스크립트 안에 적어뒀다 — 안 적으면 "
    "다음 사람이 \"조회수 붙였는데 왜 글별로 안 보이지\"에서 시간을 쓴다."
)
d.p(
    "그리고 **만들었던 로그 버킷은 지웠다.** 한 줄도 안 들어올 저장소를 남겨두면, 몇 달 뒤에 "
    "그걸 보고 '로그가 쌓이고 있겠지'라고 믿게 된다. 이 저장소가 반복해서 당한 "
    "'설정했는데 대상이 0개'가 정확히 그 모양이다.",
    bold=True,
)

# ── 7 ────────────────────────────────────────────────────────────────────────
d.h1("7. 봇이 주소를 찔러보면 남의 집 주소가 나왔다")

d.p(
    "목록 18위. 없는 주소에 점이 들어 있으면(`/wp-login.php` 같은) 우리 라우팅이 그걸 파일 "
    "요청으로 보고 S3로 흘려보낸다. S3는 없는 파일에 이렇게 답한다."
)
d.ev(
    "실측 — 배포된 사이트에 그냥 요청해봤다",
    [
        "$ curl https://<사이트>/wp-login.php",
        "403",
        '<?xml version="1.0" encoding="UTF-8"?>',
        "<Error><Code>AccessDenied</Code><Message>Access Denied</Message></Error>",
    ],
)
d.p(
    "사람에게는 뜻 없는 글자고, 봇에게는 \"여기는 S3다\"라는 대답이다. 취약점은 아니지만 "
    "공짜로 알려줄 이유도 없다."
)
d.p(
    "전체 에러페이지를 지정하는 방법은 **일부러 안 썼다.** 그건 사이트 전체에 걸려서 "
    "API가 주는 권한 거부 응답까지 덮어버린다 — 예전에 그것 때문에 '막혔는데 성공으로 보이는' "
    "상태를 만든 적이 있어서 그때 제거했던 설정이다. 대신 엣지에서 404 페이지를 **직접 만들어** 돌려준다."
)
d.p(
    "그런데 엣지에서는 파일이 실제로 있는지 알 수 없다. 그래서 '없는 것'이 아니라 "
    "**'이 사이트가 낼 수 있는 종류인가'** 로 갈랐다. 허용 목록은 실제 산출물 기준이다."
)
d.warn(
    "이걸로 다 막히지는 않는다. 허용 목록에 있는 확장자인데 파일이 없으면 여전히 원시 XML이 나간다. "
    "그건 존재를 알아야 판단할 수 있는 자리라 엣지에서는 못 고친다. 여기서 닫은 건 봇 스캔과 "
    "오타 대부분이다 — 그 한계도 코드에 적어뒀다."
)

# ── 8 ────────────────────────────────────────────────────────────────────────
d.h1("8. 앱에만 있던 규칙을 DB에 옮겨 적었다")

d.p(
    "목록 13위. 사용자를 참조하는 외래키가 아홉 개인데 **글 테이블의 작성자 열 하나만** "
    "'사용자가 지워지면 어떻게 할지'가 비어 있었다."
)
d.p(
    "어느 쪽으로 정할지는 코드를 읽어보고 알았다. 관리자 삭제 기능이 **이미** 사용자를 지우기 전에 "
    "그 사람 글을 직접 지우고 있었다. 즉 '사용자 삭제 = 글도 삭제'는 이미 이 저장소의 결정이었고, "
    "**DB에만 안 적혀 있었을 뿐**이다."
)
d.note(
    "동작을 바꾸는 수정이 아니라, 앱 코드에만 있던 규칙을 DB에도 적는 수정이다. "
    "이러면 psql로 직접 만지거나 복원 훈련을 돌리는 등 앱을 안 거치는 경로도 그 규칙 밖으로 못 나간다."
)
d.p(
    "제약 이름을 코드에 박지 않고 DB에서 조회해서 쓴다. 원래 그 이름을 우리가 아니라 "
    "Postgres가 붙였기 때문이다. 이름을 틀리면 마이그레이션이 실패하고, 그건 곧 **서버 기동 실패**다."
)

# ── 9 ────────────────────────────────────────────────────────────────────────
d.h1("9. 재빌드가 조용히 안 돌았다")

d.p(
    "여기까지 만들고 배포에 들어갔다. 백엔드 코드를 서버로 보내고, 재빌드 명령을 실행했다. "
    "그리고 검증 스크립트를 돌렸다."
)
d.ev(
    "scripts/verify_deploy.sh",
    [
        "❌ 앱 코드 불일치 — 운영 a1a302dac9e1 ≠ 로컬 b15e9fede9d2",
        "❌ 마이그레이션 불일치 — 운영 e1f2a3b4c5d6 ≠ 로컬 head a3b4c5d6e7f8",
    ],
)
d.p("서버에 들어가서 확인해봤다.")
d.ev(
    "서버 실측",
    [
        "이미지 생성시각:  2026-08-14 10:11 UTC   ← 어제 것 그대로",
        "컨테이너 가동:    Up 41 minutes          ← 부팅 시점 그대로",
        "도는 docker build: 없음",
        "보낸 마이그레이션: a3b4c5d6e7f8.py 있음   ← 코드는 잘 갔다",
    ],
)
d.p(
    "코드는 갔는데 **재빌드가 실행되지 않았다.** 명령이 중간에 끊겼는지, 아예 안 들어갔는지는 "
    "알 수 없다. 다시 실행하니 정상적으로 빌드가 돌았고 검증도 통과했다."
)
d.p(
    "여기서 중요한 건 재빌드가 한 번 실패한 게 아니라, **'했다'는 말과 '됐다'는 사실이 갈렸는데 "
    "그걸 스크립트가 잡았다**는 것이다. 어제 만든 검증이 하루 만에 값을 했다.",
    bold=True,
)
d.analogy(
    "약을 먹었는지 기억이 안 날 때, 통에 남은 알약을 세보는 것과 같다. "
    "기억은 흐리지만 개수는 거짓말을 안 한다."
)

# ── 10 ───────────────────────────────────────────────────────────────────────
d.h1("10. 내가 넣은 검사가 러너에서만 빨간불이었다")

d.p(
    "엣지 404를 만들면서 그 코드에 테스트를 붙였다. 이 파일은 사이트의 모든 정적 요청이 "
    "지나가는 자리인데 여태 아무도 검사하지 않았다 — 틀리면 증상이 '사이트 전체가 404'다."
)
d.p("내 컴퓨터에서는 7개 전부 통과했다. 그런데 CI에서는 빨간불이었다.")
d.ev(
    "CI(Node 24) — 로컬은 Node 20",
    [
        "✖ terraform (87.444664ms)",
        "  'test failed'",
        "",
        "# tests 1  fail 1      ← 내가 쓴 단언 7개는 하나도 안 돌았다",
    ],
)
d.p(
    "테스트를 **폴더**로 지정했는데, Node 22부터는 준 경로를 '테스트 파일 하나'로 취급한다. "
    "폴더를 주면 그 안을 훑지 않고 폴더 자체를 실행하려다 죽는다. 도커로 20·22·24를 나란히 "
    "돌려 재현했고, 파일 경로로 바꾸니 세 버전 전부 통과했다."
)
d.note(
    "검사를 넣은 커밋이 검사를 죽인 게 아니다. **검사가 아무것도 검사하지 않은 채 빨간불을 냈다.** "
    "빨간불이라 다행이지, 만약 이게 초록으로 났으면 '검사가 있다'고 믿으면서 아무것도 안 보는 "
    "상태로 몇 달을 갔을 것이다."
)

# ── 11 ───────────────────────────────────────────────────────────────────────
d.h1("11. \"확인했는데 모르겠어\"")

d.p(
    "배포까지 끝내고 눈으로 볼 것들을 정리해 넘겼다. 폰트가 제대로 보이는지, 칩 테두리가 "
    "자연스러운지, 코드블록 간격이 고른지. 돌아온 답이 이랬다."
)
d.p("\"확인했는데 모르겠어.\"", bold=True)
d.p(
    "맞는 반응이다. **내가 판단을 떠넘겼다.** 기준 없이 '자연스러운가'를 물으면 알 수가 없다. "
    "비교 대상이 없는데 어떻게 아나."
)
d.p(
    "그래서 방향을 바꿨다. 브라우저만이 할 수 있는 일은 **레이아웃 계산**이지 판단이 아니다. "
    "그러니 브라우저가 재게 하고, 사람은 결과만 읽으면 된다."
)
d.p(
    "실제 페이지를 숨겨진 프레임으로 열어서 계산하는 진단 페이지를 만들었다. "
    "'또렷해 보이는가' 대신 **같은 문장을 두 글꼴로 그려 폭을 비교**한다. 차이가 0이면 안 쓰이는 것이다. "
    "'테두리가 자연스러운가' 대신 **브라우저가 계산한 색을 읽어 대비를 숫자로** 낸다. "
    "'간격이 고른가' 대신 **코드블록 전부의 여백을 px로 재서** 최소·최대 차이를 뽑는다."
)
d.ev(
    "결과",
    [
        "전부 통과 — 14건",
    ],
)
d.p(
    "그리고 이 방식이 그 자리에서 한 건을 더 잡았다. 대비를 계산해보니 **태그 칩 테두리가 "
    "1.51:1**이었다. 기준은 3:1이다. 표·인용구와 같은 회색을 그대로 썼는데, 그것들은 장식 "
    "구분선이라 괜찮지만 **칩은 누르는 버튼**이다. 경계가 안 보이면 누를 수 있는 줄 모른다.",
    bold=True,
)

# ── 12 ───────────────────────────────────────────────────────────────────────
d.h1("12. 그리고 없는 숙제가 하나 있었다")

d.p(
    "확인 목록에 '소제목 바로 뒤에 오는 코드블록의 여백을 보라'는 항목이 있었다. "
    "지난 편에서 계산으로만 고치고 눈으로 못 봤다고 적어둔 자리다. 근거는 코드 주석이었다."
)
d.ev(
    "frontend/src/pages/PostDetailPage.tsx 주석",
    [
        "그러면 소제목 바로 뒤 코드블록에 없던 여백 ~27px이 생긴다",
        "— 이 블로그에서 가장 흔한 배치다.",
    ],
)
d.p("정말 그런지 세어봤다.")
d.ev(
    "content/devlog 전체를 훑어서",
    [
        "여는 코드펜스 193개 — 바로 앞에 오는 것:",
        "  보통 문단   192개  (99%)",
        "  인용문        1개  (1%)",
        "  소제목        0개  ← '가장 흔한 배치'라고 적어둔 그것",
    ],
)
d.p(
    "**0개였다.** 수정 자체는 옳다 — 여백이 새는 현상은 앞에 무엇이 오든 나므로, 그 코드가 실제로 "
    "지키는 건 '모든 코드블록의 간격'이다. 틀린 건 근거로 적어둔 문장이었다.",
    bold=True,
)
d.p(
    "그런데 그 한 문장이 값을 치르고 있었다. 오늘 확인 목록에 **없는 숙제**가 하나 올라가 있었고, "
    "나는 사람에게 있지도 않은 배치를 찾아보라고 시켰다."
)
d.note(
    "예전 검사에서 \"잡아낸 것 중 절반이 버그가 아니라 내가 쓴 문장이었다\"고 적은 적이 있다. "
    "오늘 그 문장을 다시 만났다. 코드는 돌지만 서술이 틀리면, 다음 사람이 그 서술을 믿고 움직인다."
)

# ── 13 ───────────────────────────────────────────────────────────────────────
d.h1("13. 오늘 닫은 것")

d.bullet("**폰트** — 첫 화면이 남의 CDN을 기다리지 않는다")
d.bullet("**검색·태그 필터·관련 글** — 31편에서 주제로 찾을 수 있다")
d.bullet("**대표 주소(canonical)** — 같은 글의 두 주소가 서로의 중복이 아니게 됐다")
d.bullet("**댓글 알림** — 화면 종과 기기 알림 둘 다. 누르면 댓글 자리로 간다")
d.bullet("**AI 사용량 화면** — 나흘째 세고 있던 숫자를 이제 볼 수 있다")
d.bullet("**작성자 외래키** — 앱에만 있던 규칙이 DB에도 적혔다")
d.bullet("**엣지 404** — 봇이 찔러도 S3라고 대답하지 않는다")
d.bullet("**방문 지표** — 요금제 때문에 방식은 바꿨지만, 이제 숫자가 보인다")
d.p("어제 남긴 11건이 전부 닫혔다. 남은 목록은 0건이다.", bold=True)

# ── 14 ───────────────────────────────────────────────────────────────────────
d.h1("14. 아직 안 끝난 것")

d.p("정직하게 남긴다.")
d.bullet(
    "**어느 글이 읽혔는지는 여전히 모른다.** 요청 단위 로그가 필요하고 그건 요금제를 올려야 한다. "
    "지금은 '얼마나 오는가'까지만 안다."
)
d.bullet(
    "**허용 목록에 있는 확장자인데 없는 파일**은 아직 원시 XML을 돌려준다. 엣지에서는 파일 존재를 "
    "알 수 없어서 못 고친다."
)
d.bullet(
    "**카카오톡·트위터의 공유 카드**는 여전히 전부 같다. 그 봇들은 자바스크립트를 안 돌리기 때문에 "
    "앱에서 뭘 해도 안 바뀐다. 정적 아카이브 주소로 공유하면 편마다 다른 카드가 뜬다."
)

# ── 맺음 ─────────────────────────────────────────────────────────────────────
d.h1("한 줄로 남기면")

d.p(
    "지난 편이 \"도는 것과 닿는 것은 다르다\"였다면, 오늘은 그 앞 단계다 — "
    "**처방을 붙이기 전에, 이 집에서 그게 실행되기는 하는지부터 봐야 한다.**",
    bold=True,
)
d.p(
    "오늘 세 번 같은 벽에 부딪혔다. 인터넷의 정석대로 붙였는데 이 프로젝트의 보안 설정이 "
    "조용히 막았다. 셋 다 막혀도 화면은 멀쩡해 보인다 — 폰트는 다른 글꼴로 폴백하고, "
    "필터는 안 뜨기만 하고, 알림은 소리 없이 교체된다. **에러를 안 내는 실패가 제일 오래 산다.**",
    bold=True,
)
d.p(
    "곁들임 — \"'확인했는데 모르겠어'는 사용자의 실패가 아니라 내 요청의 실패였다. "
    "기준 없이 판단을 시키면 아무도 판단할 수 없다. 눈에게 물을 것과 자로 잴 것을 내가 갈랐어야 했다.\"",
    bold=True,
)
d.p(
    "하나 더 — \"오늘 고친 것 중 하나는 코드가 아니라 문장이었다. '가장 흔한 배치다'라고 적어둔 "
    "그 배치는 193개 중 0개였고, 그 한 줄이 없는 숙제를 하나 만들어내고 있었다.\"",
    bold=True,
)

d.save()
