"""2026-06-26 개발일지(AWS 프로덕션 배포 + 이미지 S3 + 구독 + 보안 심층점검)를 Word로 → 프로젝트 + Windows host 양쪽 저장."""

import os
import shutil

from docx import Document
from docx.shared import Pt, RGBColor
from docx.oxml.ns import qn

DATE = "2026-06-26"
FILENAME = f"블로그_개발일지_{DATE}.docx"
PROJECT_PATH = f"/home/es0764/blog-platform/{FILENAME}"

doc = Document()
normal = doc.styles["Normal"]
normal.font.name = "맑은 고딕"
normal.font.size = Pt(10.5)
normal._element.rPr.rFonts.set(qn("w:eastAsia"), "맑은 고딕")


def h1(t):
    doc.add_heading(t, level=1)


def h2(t):
    doc.add_heading(t, level=2)


def p(t, bold=False):
    para = doc.add_paragraph()
    r = para.add_run(t)
    r.bold = bold
    return para


def quote(t):
    para = doc.add_paragraph()
    r = para.add_run(t)
    r.italic = True
    r.font.color.rgb = RGBColor(0x55, 0x55, 0x55)
    para.paragraph_format.left_indent = Pt(18)


def bullet(t):
    doc.add_paragraph(t, style="List Bullet")


def analogy(t):
    para = doc.add_paragraph()
    r = para.add_run("🔎 비유   ")
    r.bold = True
    r.font.color.rgb = RGBColor(0x1F, 0x6F, 0xEB)
    r2 = para.add_run(t)
    r2.font.color.rgb = RGBColor(0x1F, 0x6F, 0xEB)
    para.paragraph_format.left_indent = Pt(12)


# ===================== 표지 =====================
doc.add_heading("블로그 개발일지", level=0)
p(f"날짜: {DATE}", bold=True)
p("주제: 계정 시스템을 AWS 프로덕션에 배포 → 이미지 S3 이전 → 구독 기능 완결 → 보안 심층 점검(5회)")
p("대상: 웹/클라우드 입문자가 읽어도 이해되도록, 개념은 전문가 수준으로 + 비유로 설명")
doc.add_paragraph("")

# ===================== 0. 한눈에 =====================
h1("0. 오늘 한 일 한눈에")
p("어제 로컬에서 완성한 '계정 권한 시스템'을 실제 AWS 라이브 서버에 올렸다. 그 과정에서 배포 사고를 한 번 겪고 "
  "수습했고, 메일 발송(SES)과 이미지 저장(S3)을 제대로 붙였다. 그리고 '보안이 허술하다'는 의심에서 출발해 "
  "내 사이트를 직접 공격해보며 5번에 걸쳐 취약점을 찾아 막았다.")
bullet("AWS 프로덕션 풀 배포 — 배포 사고(프론트만 올라감) → 롤백 → 제대로 재배포")
bullet("SES(이메일 발송) 셋업 — 인증메일·비번재설정 메일을 실제로 보냄")
bullet("이미지 업로드를 S3로 이전 — 재배포·서버 교체에도 안 사라지게")
bullet("구독 기능 완결 — '이 블로그 구독' 입구 + 구독 목록")
bullet("보안 점검 5회 — 토큰 위조·서버 노출·IDOR·PII 유출 등 발견·수정")
analogy("어제가 '집을 다 지은 날'이라면, 오늘은 '실제 거리에 집을 세우고(배포) + 경비·자물쇠를 직접 털어보며 점검한 날'이다.")

# ===================== 1. AWS 배포와 사고 =====================
h1("1. AWS 프로덕션 배포 — 그리고 배포 사고")
p("로컬(내 컴퓨터)에서 잘 돌던 걸 실제 인터넷 주소로 올리는 게 '배포'다. 우리 구조는 "
  "프론트(화면)는 S3+CloudFront, 백엔드(API)는 EC2, 데이터는 RDS에 있다.")
h2("1-1. 사고: 프론트만 올라가서 어긋남")
p("코드를 GitHub에 올리자, 자동 배포(CI/CD)가 '프론트엔드'만 라이브 서버에 올렸다. 그런데 백엔드는 수동 배포라 "
  "옛날 버전 그대로였다. 결과: 새 화면이 '아직 없는 기능(API)'을 부르며 깨졌다.")
analogy("최신 리모컨(프론트)을 새로 샀는데 TV(백엔드)는 옛날 모델 그대로인 상황. 새 버튼을 눌러도 TV가 못 알아듣는다.")
p("대응을 2단계로 했다. ① 급한 불 끄기 = 프론트를 '기능 넣기 전' 버전으로 되돌려(롤백) 라이브를 일관되게 만들고, "
  "② 백엔드까지 최신으로 제대로 올린 뒤 프론트도 최신으로 다시 배포.")
h2("1-2. 전문가 노트 — 배포 절차")
p("백엔드 배포는 '코드 압축(tar) → 서버로 전송(scp) → 도커 재빌드'다. 이때 운영 DB(RDS)에는 컨테이너가 뜨면서 "
  "DB 구조 변경 이력(마이그레이션)이 자동 적용된다.")
analogy("새 가구(코드)를 트럭에 실어(tar) 매장(EC2)에 배송(scp)하고, 매장 도면(DB)도 새 도면대로 자동 시공되는 것.")
p("교훈: 프론트와 백엔드는 '버전이 맞아야' 한다. 한쪽만 올리면 어긋난다. (자동배포가 프론트만 건드리는 함정 인지.)")

# ===================== 2. SES =====================
h1("2. SES — 이메일을 실제로 보내기")
p("회원가입 인증·비밀번호 재설정 메일을 실제로 보내려면 발송 서버가 필요하다. AWS의 SES를 썼다. "
  "내 메일 주소를 '발신자'로 인증하고, 앱이 쓸 SMTP 자격증명을 발급받았다.")
analogy("SES는 '우체국 계약'이다. 아무 봉투나 막 못 보내고, 먼저 '내 반송 주소가 진짜다'를 증명(이메일 인증)해야 한다.")
h2("2-1. 샌드박스 — 도메인 없이 가는 법")
p("새 SES 계정은 '샌드박스' 상태라 '인증된 주소'에게만 메일이 간다. 누구에게나 보내려면 '프로덕션 액세스'를 "
  "신청해 심사를 받아야 한다. 또, 도메인이 없으면 gmail 주소로 발송하게 되는데, 받는 쪽 DMARC 정책 때문에 "
  "스팸함으로 갈 수 있다(도메인이 있어야 전달력이 좋아진다).")
analogy("개통 직후 업무용 번호 같은 것. 처음엔 '등록된 번호'로만 발신되고, 정식 승인 후 아무 번호로나 보낼 수 있다.")
p("결정: 포트폴리오 데모라 '샌드박스 + 테스트할 이메일만 인증' 방식으로 충분히 굴러간다.")

# ===================== 3. 보안 심층 점검 =====================
h1("3. 보안 — '허술하다'에서 출발한 5번의 점검")
p("'보안이 허술하다'는 막연한 불안을, 실제로 내 사이트를 공격해보며 항목별로 확인했다. 발견한 진짜 구멍들:")

h2("3-1. 가장 치명적 — SECRET_KEY (토큰 위조)")
p("로그인 상태는 'JWT 토큰'으로 증명한다. 이 토큰은 서버의 비밀키(SECRET_KEY)로 서명된다. 그런데 운영 서버에 "
  "이 키가 설정 안 돼 코드 기본값('change-me-in-production')을 쓰고 있었다 — 이 값은 GitHub 코드에 그대로 공개돼 있어, "
  "누구나 그 키로 '나는 관리자다' 토큰을 위조할 수 있었다. 이것이 '10초 만에 털리는' 진짜 구멍이었다.")
analogy("도장(서명키)을 잃어버린 게 아니라, 그 도장의 디자인이 인터넷에 공개돼 있어서 누구나 똑같이 파서 찍을 수 있던 상태.")
p("조치: 강력한 임의 키로 교체. 그리고 '기본값이면 서버가 아예 안 켜지게(fail-closed)' 가드를 넣어 재발을 막았다.")
analogy("공사 끝나면 마스터키를 회수하듯, 기본키가 남아있으면 출입 자체를 거부하게 만든 것.")

h2("3-2. 서버가 인터넷에 직접 노출")
p("백엔드 서버(EC2)가 IP로 직접 접속 가능해서 /docs(API 설명서)가 다 보이고, 앞단의 보안(WAF·HTTPS)을 우회할 수 있었다. "
  "→ 8000 포트를 'CloudFront만' 들어오게 잠갔다(관리형 IP 목록 사용).")
analogy("정문(CloudFront)에 경비를 세웠는데 뒷문(직접 IP)이 열려 있던 것 → 뒷문을 잠가 정문으로만 들어오게.")

h2("3-3. 남용/DoS — 입력·도배·용량")
p("글 제목을 아주 길게 넣으면 서버가 500 에러를 냈고('글 길게 쓰면 안 써짐'의 정체), 댓글은 무제한 도배가 됐고, "
  "본문·이미지 용량 제한이 없었다. → 길이 검증(친절한 422), 레이트 리밋(시간당 횟수 제한), 업로드 5MB 상한을 넣었다.")
analogy("회전문이 너무 빨리 돌면 잠기게 한 것 — 정상 손님은 영향 없고, 떼로 밀고 들어오는 시도만 막힌다.")

h2("3-4. 인증 하드닝 — 비번 길이·세션 무효화")
p("비밀번호 최소 길이(8자)를 강제하고, 비번을 재설정하면 '예전에 발급된 토큰을 즉시 무효화'하게 했다(token_version). "
  "토큰 만료도 24h→12h로 줄였다.")
analogy("비번을 바꾸면 예전 열쇠가 전부 안 먹게 자물쇠 실린더를 통째로 교체하는 것.")

h2("3-5. 더 깊게 — IDOR · 토큰 혼동 · PII 유출")
p("점검을 거듭하며 더 미묘한 것들을 찾았다:")
bullet("비공개 글의 댓글이 새어나감(IDOR): 글은 못 봐도 댓글 API는 권한 확인을 안 해서 누구나 읽혔다 → 볼 권한 있는 사람만.")
bullet("토큰 혼동: '이메일 인증/비번재설정' 토큰을 '로그인 토큰'으로 그대로 쓸 수 있었다 → 용도(purpose) 다른 토큰은 거부.")
bullet("구독자 이메일 전수 노출: 구독자 목록 API가 인증 없이 모든 이메일(개인정보)을 반환 → 관리자 전용으로 잠금.")
bullet("알림 메일 HTML 인젝션: 글 제목이 메일 HTML에 그대로 들어가 악성 코드 주입 가능 → 이스케이프 처리.")
analogy("자물쇠(인증)는 튼튼한데, 옆에 '확인용 쪽문'들이 슬쩍 열려 있던 것 — 하나씩 다 잠갔다.")

h2("3-6. 1회용 토큰 · 댓글 모더레이션")
p("비번재설정 링크를 한 번 쓰면 무효가 되게(1회용) 만들고, 글 작성자·관리자가 스팸 댓글을 삭제할 수 있게 했다.")

h2("3-7. CSP는 보류 (플랜 제약)")
p("XSS를 한 겹 더 막는 CSP 헤더를 넣으려 했으나, CloudFront '무료 요금제'가 커스텀 응답헤더 정책을 거부했다. "
  "다행히 주 방어(마크다운이 raw HTML 미렌더 + nosniff)는 이미 있어, CSP는 '있으면 좋은' 추가 방어층이라 보류했다.")

# ===================== 4. 이미지 S3 =====================
h1("4. 이미지 업로드를 S3로 이전")
p("증상: 다른 기기에서 보면 이미지가 안 떴다. 원인: 이미지가 '컨테이너 안'에 저장돼, 재배포(오늘 여러 번)할 때마다 "
  "초기화되며 사라졌다. → 우선 호스트 디스크 볼륨으로 영구화했고, 이어서 아예 S3에 저장하도록 바꿨다.")
analogy("그림을 '임시 천막(컨테이너)' 안에 걸어둔 셈 — 천막을 새로 치면 그림도 사라진다. S3 = 튼튼한 외부 창고.")
h2("4-1. 전문가 노트 — 인스턴스 역할(키 없는 인증)")
p("EC2가 S3에 올리려면 권한이 필요한데, 키를 코드/설정에 박는 건 위험하다. 그래서 'IAM 역할'을 EC2에 붙여, "
  "키 없이(인스턴스 메타데이터로) 자동 인증하게 했다. 권한도 'uploads 폴더에 올리기'만으로 최소화했다.")
analogy("직원증(역할)을 목에 걸어주면, 매번 비밀번호(키)를 들고 다니지 않아도 출입이 되는 것. 그리고 그 직원증은 '창고 한 칸'만 연다.")

# ===================== 5. 구독 =====================
h1("5. 구독 기능 완결")
p("'글쓴이 구독 → 일부공개 글 열람'이 안 된다는 신고를 추적했더니, 구독 기록이 0건 — 실제로 구독이 안 되고 있었다. "
  "원인: 구독 버튼이 '글 상세'에만 있는데, 일부공개 글은 구독 전엔 못 열어서(404) 구독할 입구가 없었다(막다른 길).")
analogy("회원만 들어가는 방인데, 회원가입 창구가 그 방 안에만 있는 셈 — 들어갈 수가 없으니 영영 회원이 못 된다.")
p("해결(개인블로그형): 홈에 '이 블로그 구독' 버튼을 둬서 주인장을 한 번에 구독 → 일부공개 글이 열리게. "
  "그리고 '내가 구독 중인 블로그' 목록과 구독 취소도 보이게 했다.")

# ===================== 6. 버그 =====================
h1("6. 잡은 버그들")
bullet("새로고침하면 로그인창으로 튕김 — 인증 복구가 끝나기 전에 '로그인 안 됨'으로 판단하던 것 → 복구 대기.")
bullet("메일 링크가 클릭 안 됨 — 순수 텍스트 메일을 HTML(버튼+URL)로 바꿔 클릭 가능하게.")
bullet("새 글 알림 링크 깨짐 — 상대경로/잘못된 라우트를 절대 URL·정상 경로로.")
bullet("배포가 S3 이미지를 지울 뻔 — 프론트 배포(s3 sync --delete)가 같은 버킷의 업로드 이미지를 지우는 함정 → 제외 처리.")

# ===================== 7. 개념 =====================
h1("7. 오늘 익힌 핵심 개념")
bullet("배포 일관성: 프론트/백엔드 버전이 맞아야 한다(자동배포의 부분 배포 함정)")
bullet("토큰 보안: 서명키 관리(기본값 금지·fail-closed), 토큰 용도 분리, 세션 무효화(token_version)")
bullet("접근제어(IDOR): '존재 확인'이 아니라 '볼 권한'을 매 요청 확인")
bullet("최소권한·키 없는 인증: IAM 역할 + IMDSv2로 SSRF에도 안전")
bullet("정직한 보안: 한 번에 끝이 아니라, 직접 공격해보며 층층이 막는다(defense in depth)")

# ===================== 8. 다음 =====================
h1("8. 다음 할 일")
bullet("GitHub push (로컬이 원격보다 여러 커밋 앞섬)")
bullet("(선택) 도메인 + SES 프로덕션 액세스 → 진짜 공개 가입·좋은 메일 전달력")
bullet("(선택) CSP를 CloudFront Functions로 주입(무료 플랜 우회), 구독 double opt-in")
bullet("(선택) S3용 IAM 역할을 Terraform으로 코드화(현재 CLI 생성분)")

# ===================== 저장 =====================
doc.save(PROJECT_PATH)
print(f"[저장] 프로젝트(WSL): {PROJECT_PATH}")

host_user = "/mnt/c/Users/erert"
cands = [
    os.path.join(host_user, "Desktop"),
    os.path.join(host_user, "OneDrive", "Desktop"),
    os.path.join(host_user, "바탕 화면"),
    os.path.join(host_user, "Documents"),
    host_user,
]
host_dir = next((d for d in cands if os.path.isdir(d)), host_user)
host_path = os.path.join(host_dir, FILENAME)
shutil.copy2(PROJECT_PATH, host_path)
print(f"[저장] Windows host: {host_path}")
