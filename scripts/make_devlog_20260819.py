#!/usr/bin/env python3
"""개발일지 #35 (2026-08-19) — 고친 자리 옆은 안 쓸린다.

서식은 scripts/devlog_doc.py가 맡는다. 이 파일엔 내용만 있다.

글투 지침(docs/voice/style-guide.md)을 지켜 쓴 두 번째 편이다. #34에서 배운 대로
문장 길이가 제일 어렵고, 그건 초고를 쓴 다음 짧은 문장을 이어 붙이는 작업으로만
해결된다. 쓰고 나면 scripts/writing_style_check.py로 재고 넘으면 고친 뒤에 발행한다.

실행 (WSL 파이썬에 python-docx가 없어 컨테이너로):

    docker run --rm -v "$PWD":/w -v "/mnt/c/Users/USER/Desktop/개발일지":/desktop \\
      -w /w python:3.12-slim sh -c "pip install -q python-docx && python scripts/make_devlog_20260819.py"
"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from devlog_doc import DevlogDoc  # noqa: E402

d = DevlogDoc("2026-08-19", "블로그 만들기 #35")

d.cover(
    subject="고친 자리 옆은 안 쓸린다",
    audience="입문자가 읽어도 이해되게. 새 기능을 만들 때 결함이 어디에 생기는지,"
    " 그리고 왜 하필 이미 고쳐둔 자리 바로 옆에 생기는지 위주로.",
    note="오늘은 기능 두 개를 만들고 검사를 두 번 돌렸다. 검사가 찾은 것 가운데 절반이"
    " 오늘이나 어제 내가 만든 것이었고, 그 이야기가 이 편의 대부분이다.",
)

# ── 1 ────────────────────────────────────────────────────────────────────────
d.h1("1. 어제 기록이 이미 틀려 있었다")

d.p(
    "아침에 이어서 하려고 어제 남긴 기록을 폈더니 '남은 건 프론트 배포 하나'라고 적혀 "
    "있었는데, 실제로 재보니 이미 배포가 끝나 있었다. S3에 남는 배포 스탬프의 커밋 "
    "해시가 어제 마지막 커밋과 같았고 라이브 피드에도 34편이 들어 있었으니, 내가 기록을 "
    "쓴 뒤에 사용자가 배포 버튼을 눌렀던 것이다."
)
d.p(
    "이 저장소에서 낡은 목록에 속은 것이 이번이 다섯 번째쯤 된다. 그래서 오늘부터는 "
    "기록을 읽는 대신 스탬프를 먼저 보기로 정했는데, 기록은 사람이 쓴 시점에 멈춰 있고 "
    "스탬프는 실제로 나간 것을 말하기 때문이다. **믿을 것은 사람이 적어둔 상태가 아니라 "
    "기계가 남긴 흔적이다.**"
)

# ── 2 ────────────────────────────────────────────────────────────────────────
d.h1("2. CSS를 몰라도 꾸미게 만들기")

d.p(
    "어제까지 블로그 외형을 바꾸는 방법은 설정 화면의 입력칸에 CSS를 직접 쓰는 것 하나뿐이었다. "
    "프리셋 세 개가 벽을 잠깐 낮춰주기는 하지만 프리셋은 고르는 것이지 꾸미는 것이 아니라서, "
    "오늘은 눌러서 바꾸는 화면을 만들었다. 강조색과 바탕색, 모서리 모양, 목록 배치, 목록에 "
    "무엇을 보일지, 사이드바와 머리말까지 열 가지를 버튼과 체크박스로 정할 수 있다."
)
d.p(
    "만들면서 제일 오래 고민한 것은 누른 값을 어디에 저장하느냐였다. 컬럼을 하나 추가하는 "
    "것이 자연스러워 보이지만 그러면 마이그레이션과 스키마와 라우터와 캐시가 전부 따라 "
    "움직이고, 무엇보다 서버를 켜서 배포해야 한다. 이 블로그는 비용 때문에 서버를 평소 "
    "꺼두므로 서버를 켜야만 닿는 처방은 실제로는 한동안 안 닿는 처방이다."
)
d.p(
    "그래서 옵션을 이미 있는 CSS 칸 **안에** 넣기로 했다. 저장되는 CSS 맨 위에 주석 한 줄로 "
    "무엇을 눌렀는지 적어두고 그 아래에 그 결과를 쓴 다음, 사람이 직접 쓴 CSS는 그 뒤에 "
    "이어 붙인다. 서버와 방문자와 캐시는 이것을 그냥 CSS로 보기 때문에 백엔드는 한 줄도 안 "
    "바뀌었고, 프론트만 배포하면 바로 닿았다."
)

d.ev(
    "저장되는 CSS의 모양",
    [
        '/*@skin-options {"accent":"#20c997","list":"grid",...}*/   <- 무엇을 눌렀나',
        ":root { --color-accent: #20c997; ... }                    <- 그 결과",
        "/*@skin-end*/",
        "(여기부터 사람이 직접 쓴 CSS. 뒤에 오므로 위를 덮을 수 있다)",
    ],
)

d.p(
    "순서를 이렇게 둔 데에도 이유가 있다. CSS는 같은 우선순위면 뒤에 온 것이 이기므로, "
    "손으로 쓴 한 줄은 언제나 클릭으로 만든 값을 덮을 수 있다. 반대로 뒀다면 CSS를 아는 "
    "사람이 자기가 쓴 규칙이 왜 안 먹는지 모른 채로 갇히게 되는데, 쉬운 입구를 만들면서 "
    "원래 있던 입구를 막는 것은 개선이 아니다."
)

d.h2("만들었더니 목록 화면에서만 통했다")

d.p(
    "다 만들고 눌러보니 잘 도는 것 같았는데, 글을 하나 열어보고 나서야 문제를 알았다. "
    "스킨이 화면을 붙잡으려면 마크업에 약속된 이름표가 붙어 있어야 하는데 그 이름표가 "
    "글 상세 화면에는 하나도 없었다. 세어보니 헤더에 하나, 사이드바 겉면에 하나, 목록에 "
    "셋이 전부였다."
)
d.p(
    "그래서 이름표를 열 개에서 스물아홉 개로 늘렸다. 헤더와 본문 칸과 푸터, 사이드바 안쪽 "
    "카드 셋, 그리고 글 상세의 제목과 본문과 목차와 댓글까지 붙였다. 글 상세의 이름은 목록과 "
    "일부러 다르게 지었는데, 같은 이름을 쓰면 목록 제목을 키우려고 쓴 한 줄이 본문 제목까지 "
    "같이 키워서 사용자가 건드린 적 없는 자리가 바뀌기 때문이다."
)
d.p(
    "그리고 이름 목록이 세 군데에 흩어져 있는 것을 하나로 모았다. 마크업과 설명 주석과 화면 "
    "안내 문구가 각자 목록을 들고 있었는데, 이름표를 열 개 늘리면 갈라질 자리가 그만큼 늘고 "
    "**갈라져도 아무것도 안 깨지기 때문에 눈으로는 못 잡는다.** 안내에만 있는 이름은 CSS를 "
    "써놓고 왜 안 먹느냐에 갇히게 하고, 마크업에만 있는 이름은 아무도 안 쓴다. 지금은 목록이 "
    "파일 하나에 있고 화면 안내가 그것을 그리며, 테스트가 목록과 마크업을 양쪽으로 대조한다."
)

# ── 3 ────────────────────────────────────────────────────────────────────────
d.h1("3. 검사 한 번에 스물넷, 살아남은 것은 열하나")

d.p(
    "기능을 두 개 만들고 나서 지난 검사 이후 쌓인 스물세 개 커밋을 훑었다. 여섯 갈래로 "
    "나눠 찾게 하고 갈래마다 반박하는 쪽을 따로 붙였는데, 지적 스물네 건 가운데 열세 건이 "
    "반박당해 죽고 열한 건이 살아남았다. 반박 쪽에는 애매하면 죽이라고 걸어뒀는데, 그럴듯한데 "
    "틀린 지적이 하나 끼면 진짜 지적까지 같이 못 믿게 되기 때문이다."
)
d.p(
    "결과를 늘어놓고 보니 결함이 한자리에 몰려 있었다. 새로 짠 검증 코드 안쪽이 아니라, "
    "**새로 뚫은 입구가 이미 있던 가드 옆을 스쳐 지나간 자리**였다."
)

d.ev(
    "터미널, 같은 요청 모양인데 답이 갈린다",
    [
        "$ curl -s -o /dev/null -w '%{http_code} %{content_type}' 'localhost:8000/api/posts?tag=a%00b'",
        "422 application/json",
        "",
        "$ curl -s -o /dev/null -w '%{http_code} %{content_type}' 'localhost:8000/api/posts?author=a%00b'",
        "500 text/plain; charset=utf-8",
    ],
)

d.p(
    "글자 사이에 NUL 바이트 하나를 끼워 넣은 요청인데, 검색어로 보내면 정중한 422가 오고 "
    "글쓴이 이름으로 보내면 인증 없이 500이 난다. 데이터베이스 드라이버가 그 문자를 만나면 "
    "DB에 닿기도 전에 던지는데 그 예외를 잡는 핸들러가 없어서 그대로 새어 나가는 것이다."
)
d.p(
    "재밌는 것은 처방이 이미 두 줄 위에 있었다는 점이다. 그 자리에는 검색어와 태그를 막는 "
    "가드가 멀쩡히 적혀 있었고, 주석에는 '짝지어진 파라미터 중 한쪽만 안 쓸렸다'는 같은 "
    "모양을 두 번이나 겪고 고쳤다는 기록까지 남아 있었다. 그 함수에서 세 번째가 난 것이다."
)
d.p(
    "손으로 적는 목록이 문제라고 판단해서 가드를 함수 하나로 옮겼고, 문자열을 받는 무인증 "
    "입구 세 곳을 전부 그 함수에 넘기게 했다. 이제 파라미터가 늘면 인자를 하나 더 넘기는 "
    "것으로 끝난다."
)

d.h2("어제 만든 것이 어제 고친 것을 되돌렸다")

d.p(
    "살아남은 열한 건 가운데 셋이 어제나 오늘 내가 만든 것이었고 그중 둘은 되돌림이었다. "
    "하나는 CSS 입력칸에서 줄바꿈이 아예 안 되는 것이었는데, 옵션을 CSS 안에 넣으면서 값을 "
    "다듬는 코드가 키를 칠 때마다 돌아 방금 친 개행을 도로 잘라내고 있었다. 붙여넣기 말고는 "
    "CSS를 쓸 수 없는 상태였는데도 화면은 멀쩡해 보였다."
)
d.p(
    "다른 하나는 다크모드에서 기본 버튼의 글자가 안 보이는 것이었다. 색을 변수로 빼면서 "
    "버튼 배경은 변수를 타게 했는데 그 위의 글자색은 흰색으로 박힌 채로 남았고, 어두운 "
    "모드에서는 강조색이 밝은 쪽으로 옮겨지므로 밝은 바탕에 흰 글자가 된다. 대비를 재보니 "
    "2.0 대 1이었고 권장 최소가 4.5 대 1이니 절반도 안 된다."
)
d.p(
    "여기서 내가 놓친 방식이 마음에 걸렸다. 어제 스크린샷을 찍어서 눈으로 확인했는데도 "
    "못 봤는데, 찍은 것이 밝은 모드였고 어두운 모드는 스킨을 안 건 기본 상태로만 봤기 "
    "때문이다. **바꾼 축과 확인한 축이 엇갈린 조합이 빈칸으로 남았다.** 지금은 색을 정하면 "
    "그 위에 얹을 글자색도 같이 정해지고, 밝기는 눈대중이 아니라 상대휘도로 잰다."
)

d.h2("카드 격자에 남던 줄 하나")

d.p(
    "목록을 두 열 카드로 바꾸는 옵션이 지우려던 구분선을 못 지우고 있었다. 쓰는 CSS 도구가 "
    "긋는 선이 위쪽이 아니라 아래쪽인데 내가 위쪽만 지우고 있었던 것인데, 여기서 진짜 문제는 "
    "이걸 어제 스크린샷으로 못 잡았다는 쪽이다."
)
d.p(
    "개인 블로그 주소 화면에서는 멀쩡해 보였기 때문이다. 그쪽은 마크업 구조가 한 겹 얇아서 "
    "카드에 붙인 테두리가 그 선을 우연히 덮어버렸고, 사이트 본 화면에서는 등장 애니메이션을 "
    "위한 요소가 한 겹 끼어 있어서 안 덮였다. 같은 CSS가 두 화면에서 다르게 보이는데 나는 "
    "멀쩡한 쪽만 찍었던 셈이다."
)

# ── 4 ────────────────────────────────────────────────────────────────────────
d.h1("4. 보안검사, 그리고 내가 발행한 글에 있던 것")

d.p(
    "지난 보안검사 이후로 일흔여섯 개 커밋이 쌓여 있어서 한 번 더 돌렸다. 이번에는 읽지 말고 "
    "실제로 쏘라고 걸었는데, 로컬에 스택이 떠 있으니 요청을 보내고 받은 응답을 근거로 적게 "
    "하는 편이 추론보다 낫다고 봤기 때문이다. 결과는 일곱 건이었고 권한을 넘어서는 것은 "
    "하나도 없었지만, 급한 것이 하나 있었다."
)
d.p(
    "발행된 개발일지 안에 운영 실제 값이 들어 있었다. 이 글은 터미널 출력을 그대로 싣는 "
    "형식이고 그게 이 기록의 값어치인데, 데이터베이스에서 사용자 표를 조회한 화면이나 공인 "
    "IP를 확인한 응답이 마스킹 없이 같이 실렸던 것이다. 실제 계정 주소 세 개와 집 공인 IP "
    "두 개가 라이브에 나가 있었고 피드와 검색 인덱스에도 들어가 있었다."
)
d.p(
    "IP 쪽 실제 위험은 크지 않다. 접속을 허용하는 목록에 있는 것이 내 주소이고, 출발지를 "
    "위조해도 연결은 성립하지 않으며 키 인증이 남아 있기 때문이다. 남는 것은 보안보다 "
    "**개인정보 쪽인데, 나 말고 다른 두 사람의 주소가 동의 없이 공개돼 있었다.** 그래서 "
    "문서용으로 예약된 값으로 바꾸고 원본을 만든 스크립트까지 같이 고쳤다."
)
d.p(
    "고치는 것보다 중요한 것은 다시 안 새게 하는 쪽이라, 발행 전에 기계가 보는 검사를 "
    "만들어 붙였다. 이메일과 공인 IP와 접근 키와 서버 주소는 막고, 자원 번호처럼 자격증명이 "
    "아닌 것은 알리기만 한다. 사람 눈으로는 계속 새는데, 쓸 때는 그게 화면 캡처의 일부로 "
    "보이고 알아챌 때는 이미 발행된 뒤이기 때문이다."
)

d.h2("차단했는데 블로그는 그대로 열려 있었다")

d.p(
    "두 번째로 값있었던 지적은 계정을 차단해도 그 사람의 공개 블로그가 안 닫히는 것이었다. "
    "차단은 역할을 바꾸고 토큰을 무효로 만들어서 쓰기는 확실히 얼리는데, 그 사람 주소를 여는 "
    "화면이 읽는 경로 셋이 전부 인증 없이 정상 응답을 주고 있었다. 표시명과 스킨과 본인이 "
    "쓴 문장과 공개 글이 계속 나갔으니 조치가 절반만 들은 셈이다."
)
d.p(
    "고치는 길이 둘이었다. 차단할 때 주소와 스킨과 문장을 같이 지우는 방법이 있고, 공개 읽기 "
    "경로가 역할을 보게 하는 방법이 있다. 뒤쪽을 골랐는데, 앞쪽은 같은 상태를 두 군데에 두는 "
    "것이라 회수 경로가 하나 늘 때마다 같이 안 고치면 또 어긋나기 때문이다. 그게 이번에 난 "
    "사고의 모양 그대로다."
)
d.p(
    "그리고 오늘 아침에 내가 좁혀둔 것을 하나 되돌렸다. 승인 안 난 계정이 주소를 미리 "
    "차지하는 것을 막으려고 주소 설정을 전부 잠갔는데, 그랬더니 차단된 사람이 자기 주소를 "
    "내릴 방법까지 사라졌다. **들어오는 문을 잠그면서 나가는 문까지 잠근 것이라, 지금은 "
    "정하는 것만 권한이 필요하고 지우는 것은 언제나 된다.**"
)

# ── 5 ────────────────────────────────────────────────────────────────────────
d.h1("5. 가끔 깨지는 테스트를 믿을 수 없게 되기 전에")

d.p(
    "작업 도중에 컴퓨터가 두 번 갑자기 꺼졌고 도커도 같이 죽어서 백엔드 테스트를 못 돌린 채로 "
    "커밋을 올렸다. 커밋 메시지에 못 돌렸다고 적고 CI에 맡겼는데, 올려보니 정말로 빨간불이 "
    "떴다. 다행히 도커가 한 번 더 살아나서 원인을 볼 수 있었다."
)
d.p(
    "깨진 것은 한 건이었고 그것만 따로 돌리면 통과했다. 파일 단위로 돌려도 통과하고 전체를 "
    "돌릴 때만 가끔 깨지는 종류였는데, 내가 만든 결함이 아니라 원래 흔들리던 테스트였다."
)

d.ev(
    "터미널, 전제부터 확인해보니",
    [
        "$ python -c \"...select now() 두 번...\"",
        "한 트랜잭션 안 now() 두 번: True",
    ],
)

d.p(
    "데이터베이스에서 현재 시각을 묻는 함수는 한 트랜잭션 안에서 값이 고정된다. 그리고 이 "
    "저장소의 테스트는 격리를 위해 테스트 하나를 트랜잭션 하나로 묶는다. 그러니 한 테스트에서 "
    "만든 글은 작성 시각이 전부 같고, 정렬 기준이 작성 시각 하나뿐이면 동점의 순서는 "
    "데이터베이스 마음이라 실행할 때마다 달라진다."
)
d.p(
    "같은 모양이 세 곳에 있었는데 목록 쪽이 더 나빴다. 목록은 몇 개씩 끊어서 가져오는 방식이라 "
    "정렬이 완전히 정해지지 않으면 **경계에서 같은 글이 두 페이지에 나오거나 한 글이 통째로 "
    "건너뛰어진다.** 테스트 안에서만 나는 일도 아니어서, 같은 초에 두 편을 올리면 운영에서도 "
    "난다. 셋 다 글 번호로 동점을 깨고 그 흔들림을 테스트로 잠갔다."
)
d.p(
    "이걸 그냥 뒀으면 앞으로 빨간불이 떠도 또 그거겠지 하고 넘겼을 것이다. 검사가 가끔 "
    "거짓말을 하면 고장 난 것은 그 테스트 하나가 아니라 빨간불이라는 신호 전체다. 오늘 만든 "
    "기능 두 개보다 이쪽이 값있었다고 본다."
)

d.h2("오늘 남긴 것과 안 한 것")

d.p(
    "커밋 다섯 개를 올렸고 CI는 전부 초록이다. 눌러서 꾸미는 화면과 스물아홉 개로 늘어난 "
    "이름표는 이미 라이브에 나갔고, 검사가 찾은 것 가운데 프론트 쪽도 같이 나갔다."
)
d.p(
    "안 한 것도 적어둔다. 백엔드 수정은 서버를 켜야 나가는데 오늘은 안 켰다. 서버가 꺼져 "
    "있는 동안에는 그 경로에 닿을 사람이 없고, 재빌드 도중에 컴퓨터가 또 꺼지면 서버가 반쯤 "
    "올라간 상태로 남기 때문이다. CSS 규칙 하나를 우회할 수 있는 것도 알면서 안 막았는데, "
    "닿는 사람이 초대받은 사람뿐이고 처방이 정상 CSS를 깨기 때문이다. 컴퓨터는 내일 "
    "서비스센터에 맡긴다."
)

d.save()
