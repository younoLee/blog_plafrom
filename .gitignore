# 환경변수 — 절대 커밋 금지
.env
*.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# 업로드된 이미지 (로컬 저장, 커밋 제외 — 나중에 S3)
backend/uploads/

# Node
node_modules/
dist/
.DS_Store

# Terraform (나중에 추가될 때를 대비)
*.tfvars
.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.lock.info
