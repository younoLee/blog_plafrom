# ROADMAP

> **"뭘 해야 하지?"가 올 때 이 파일을 먼저 본다.**
> 목표는 클라우드 직군 취업(졸업 2027~2028). 새로 찾지 말고 아래 안 채워진 칸을 한다.

_최종 갱신: 2026-09-05_

> ⚠️ **2026-09-02 정리.** 이 파일은 08-09자로 표시돼 있었는데 아래에 08-28 결정(EIP)이
> 들어 있었다. 즉 표시된 날짜가 문서 안의 내용보다 낡아 있었다. 같이 정리하면서 개강(09-01)이
> 지나 성립하지 않게 된 서술을 그 자리에서 무효화하고, **이 문서가 알 수 없는 것은
> 빈칸으로 남겼다.** 빈칸은 `☐ 사용자가 채울 것`으로 표시했고 그때 셋이었다.
> 사실 확인이 가능한 자기모순(알림·ECS·구독 테이블)은 실측해서 한쪽으로 정리했다.
>
> ✅ **2026-09-05: 빈칸 셋이 사용자 답으로 채워졌다.** 값은 (1) SAA 미응시·예약 없음,
> (2) 학기 중 주당 5시간 이하, (3) 동계 인턴 미정. **이 셋이 아래 우선순위를 바꾼다** —
> 주당 5시간은 '새 걸 만드는 시간'이 아니라 '있는 걸 안 죽이는 시간'이라, 학기 중에는
> 자격증 하나와 개발일지만 남기고 나머지를 전부 뒤로 민다.

---

## 지금 어디쯤인가

클라우드 신입에게 보는 건 셋뿐이다. 공고에 기술이 20개씩 적혀 있어서 넓어 보이는 것.

| 관문 | 필요한 것 | 상태 |
|---|---|---|
| **서류** | AWS 자격증(SAA 등) | ❌ **미취득 · 응시도 예약도 없음** (2026-09-05 확인) |
| **면접 1: 뭘 만들었나** | 깊게 운영해본 프로젝트 1개 | ✅ 복원·알림(07-22) + **ECS 오케스트레이션(07-24 증명 3/3)** |
| **면접 2: 설명할 수 있나** | 결정과 이유의 기록 | ✅ **개발일지 42편 + PROGRESS.md** |

**두 번째·세 번째는 갖췄다.** 대부분의 신입이 "트러블슈팅 경험"에 구체적으로 답하지 못하는데,
날짜·수치·커밋이 다 남아 있다. 이걸 자산으로 인식할 것.

**그래서 지금 병목은 기술이 아니라 자격증 하나다.** 07-22에는 "급하진 않음"이라고 적었는데,
그때는 앞의 두 칸이 비어 있어서 상대적으로 그랬던 것이다. 지금은 순서가 뒤집혔다.

---

## 지난 방학 (2026-07 ~ 08-31, 09-01 개강으로 닫힘)

이 절은 원래 "이번 방학"이었다. 개강이 지났으므로 **이제 진행 중인 계획이 아니라 기록**이다.
아래 체크된 항목은 그대로 두고, 안 끝난 것은 학기 절로 옮기거나 무효화했다.

기준이었던 것: 긴 시간이 통째로 필요한 것만 여기서 하고 짬짬이 되는 건 학기로 미룬다.

- [x] **블로그 마무리** — 30분 ✅ 2026-07-22
  - 개발일지 #17 발행 완료 → 연재 17편 (라이브 `total:17, index:17` 확인)
  - backend deps PR 병합 + 배포 완료 (그 과정에서 bcrypt 5.0 회귀를 배포 전에 잡음)
- [x] **복원 훈련 (DR drill)** — 반나절 ✅ 2026-07-22 (`scripts/restore_drill.sh`)
  - 개수 대조를 넘어 **구조 검사**로 판정한다: 테이블 집합·인덱스·제약·시퀀스·
    alembic 버전·확장·쓰기 가능·BYOK 복호화. 행 수는 백업 이후 쓰기가 있으면
    정상인데도 어긋나므로 방향으로 분류(DRIFT)하고 불합격시키지 않는다.
  - **측정값**: 복원 656ms, 전송 포함 RTO 약 2초. RPO는 '마지막 정지 시점'
    (백업이 cron이 아니라 정지 절차에서 돈다).
  - **면접에서 쓸 것**: 여기서 나온 게 "훈련을 만들었다"가 아니라 **훈련이
    통과시키던 13가지 구멍**이다 — 테이블 12개 중 5개만 보고 있었고, 키가 없어도
    "통과"로 찍는 오판정이 있었고, 실패하면 훈련용 DB가 운영 서버에 남았다.
  - 더 나아간 것: `RECOVERY.md` 런북 + **임시 인스턴스로 맨바닥 재건 리허설**.
    거기서 문서 버그 2개를 찾았다(buildx 미설치로 빌드 불가 / `.dockerignore`
    누락으로 시크릿이 이미지에 구워지고 앱이 재시작 루프).
- [x] **알림** — 반나절 ✅ 2026-07-22 (`scripts/watch.sh` + `.github/workflows/watch.yml`)
  - CloudWatch+SNS가 아니라 **매시 도는 GitHub Actions**로 만들었다. 이유가 있다:
    이 서버는 필요할 때만 켜므로 "백엔드가 죽었다"를 그냥 알람으로 걸면 **끌 때마다
    울린다**. 진짜 이상은 "EC2가 켜져 있는데 공개 API가 죽음"이라는 **조합**이고,
    그건 AWS 안이 아니라 **바깥에서** 봐야 안다 — 앱 자가점검은 `backend_ok`가
    하드코딩 `True`라 27,506번 중 실패가 0건이었고 CloudFront를 아예 못 본다.
  - 지금 보는 것: EC2 running인데 공개 API가 죽었는가 / 6시간 이상 켜둔 채 방치 /
    최신 백업 나이 / 만료 안 되는 사본 / 이미지 사본 / SES 샌드박스 여부.
  - **효과 실증**: 첫 실행에서 실제 문제 2건을 그대로 잡았다 — 오리진이 주차된 채
    3시간 방치된 것과, **SES 프로덕션 액세스가 4주째 거부 상태**인 것.
    - SES 쪽은 **2026-08-04에 종결**했다(두 번 신청·두 번 거절 → 샌드박스로 계속 운영).
      그래서 이 검사는 이제 "고칠 문제"가 아니라 **"샌드박스로 남아 있는지"를 지키는
      쪽**이다 — `SES_SANDBOX_EXPECTED=true`가 그 뜻이고, 바꾸지 않는다.
      경위: `docs/ses-production-access.md`.
  - <ins>(**2026-09-02 정정**) 여기 "남은 것: AWS 네이티브 경로(SNS + EC2 상태검사 알람)를
    추가하면 …"이 적혀 있었는데, 그건 **이 문서 아래쪽에서 이미 완료로 적힌 항목**이다
    (`terraform/alerts.tf`, 2026-08-09). 한 문서가 같은 일을 '남았다'와 '끝났다'로 동시에
    말하고 있었고, 이 저장소가 ECS 게이팅에서 똑같이 겪은 모양이다. 남은 것은 없다.</ins>
- [x] **컨테이너 오케스트레이션 (ECS)** — ✅ **2026-07-24 하루에 끝났다** (2~3주로 잡았던 칸)
  - 계획서 `docs/ecs-migration-plan.md`(단계 0~7) → 실배포·컷오버 → **증명 3/3** → tear down.
  - 확정대로 갔다: Fargate · RDS db.t4g.micro Single-AZ · "세우고→증명하고→정리".
  - **증명 3/3 실측** (`docs/ecs-stage6-evidence.md`): ①태스크 kill → ECS 자동복구(프로버 5xx 0건)
    ②무중단 롤링 배포(배포 구간 229/229 = 200) ③오토스케일 desired 2→4 증설.
    ③은 정직하게 적어둘 것 — 랩탑 한 대로는 CPU 60%를 못 태워(16,807req에 ~10%)
    **타깃을 1%로 낮춰서** 알람→정책→증설을 실증했다. 그 사정까지가 재료다.
  - **실비용 ~$1 미만**(한나절). ALB 월 $16을 피한 게 "세우고→증명하고→정리"의 요점.
  - 남은 흔적: terraform 코드는 working tree에 있고 리소스만 지워졌다.
    ⚠️ ~~**타깃 없이 `terraform apply` 하면 스택이 재생성된다(~$50/mo).**
    `backend_image_tag`가 required var라 speed bump가 있고,~~ `var.enable_ecs` count 게이팅이
    **붙어 있다**(2026-07-27부터. 아래 '작은 남은 칸 둘' 참고).
    <ins>(**2026-09-02 정정** — 지운 두 문장이 지금 코드와 안 맞는다. `enable_ecs`의 기본값이
    `false`이고(`terraform/ecs.tf`의 `variable "enable_ecs"`) ALB·RDS·ECS 리소스에 전부
    `count = var.enable_ecs ? 1 : 0`이 붙어 있으므로, 인자 없는 실행은 이 스택을 만들지 않는다.
    그리고 `backend_image_tag`는 더 이상 required가 아니다. 07-27 게임데이가 그 required 때문에
    복구 1번 명령이 죽는 것을 밟고 `default = ""`를 줬다(`terraform/ecs.tf:9-22`). 즉 speed bump는
    모든 실행이 아니라 **태스크를 실제로 만들 때의 precondition**으로 옮겨졌다. 되살릴 때는
    `-var="enable_ecs=true" -var="backend_image_tag=<git SHA>"` 둘을 같이 준다.)</ins>
    > **(2026-08-10 정정)** 이 줄은 08-09까지 "게이팅은 미적용"이라고 적혀 있었다. 그런데
    > **같은 파일 20줄 아래**에서 이미 "그건 틀린 서술이었다"고 정정해놓은 상태였다.
    > 한 문서가 자기 자신과 모순된 채 하루를 넘긴 것이라, 아래를 안 읽고 위만 읽은 사람은
    > 여전히 "타깃 없이 apply하면 $50/mo 스택이 산다"고 믿게 된다. **정정은 원문 자리에서
    > 해야 한다** — 새 줄을 밑에 추가하는 것만으로는 낡은 줄이 안 죽는다.
  - 오리진 HTTPS는 여전히 안 풀렸다 — 커스텀 도메인+ACM이 필요한데 도메인을 안 사기로 했다(아래).

- [ ] **SAA 취득** ← 방학에 남은 유일한 큰 칸이었다. 방학이 닫혔으므로 학기 절로 넘어간다.
  - 자투리 항목이었다가 본체가 됐다. ECS가 끝나면서 이 자리가 비었다.
  - ⚠️ ~~**07-22에 적은 "8월 말~9월 초 응시" 목표는 이제 성립하지 않는다.** 그때는 개강을
    09-07로 알고 있었는데 **09-01이 맞다**(2026-08-09 재확인). '9월 초'는 개강 뒤다.
    → 방학 안에 치려면 **8월 말이 사실상 마지막**이고, 남은 준비 기간은 3주다.~~
    <ins>(**2026-09-02**) 이 계산은 08-09 기준이라 이제 아무것도 안 가리킨다. 개강일 09-01은
    지났고 "개강까지 23일"도 "8월 말이 마지막"도 성립하지 않는다. 응시했는지, 예약을 잡았는지는
    이 저장소에 근거가 없으므로 여기서 단정하지 않는다.

    ✅ **(2026-09-05 확인) 아직 안 쳤고 예약도 없다.** 08-09의 '미취득'에서 27일 동안
    바뀐 것이 없다. 07-22·08-09·09-02에 이어 **네 번째로 같은 줄이 반복되고 있다** —
    이쯤이면 "공부 시간이 부족하다"가 아니라 **예약을 안 잡은 것 자체가 원인**이다.
    → 다음 행동은 공부가 아니라 **결제·예약 한 번**이다. 시험은 언제든 무료 1회 연기가
    되므로(응시일 48시간 전까지), 날짜를 먼저 잡고 준비를 거기 맞추는 쪽이 손해가 없다.</ins>
  - ECS와 시험범위가 겹친다(VPC·서브넷·ALB/타깃그룹·헬스체크·Auto Scaling·IAM 역할).
    **그 겹치는 부분은 책이 아니라 손으로 이미 했다** — 07-24에 직접 세우고 증명했다.
  - 안 겹치는 부분이 공부할 곳: RDS Multi-AZ/복제본, DynamoDB, SQS/SNS/Kinesis,
    Route 53 라우팅 정책, EFS, S3 스토리지 클래스, Organizations.
  - → **날짜를 먼저 예약한다.** 07-22에도 "예약하면 밀리지 않는다"고 적어놨는데
    08-09까지 안 잡혀 있었고, 같은 줄이 3주째 반복되고 있다는 게 그때의 신호였다.
    **09-05 현재도 예약이 없다**(위 (1)). 같은 줄의 네 번째 반복이다.

**작은 남은 칸 둘** — 2026-08-09에 둘 다 닫혔다. 하나는 **이미 닫혀 있었다.**

- [x] **`var.enable_ecs` count 게이팅** — ✅ **이미 되어 있었다(2026-07-27 DR 게임데이).**
  RDS·ALB·ECS 서비스·태스크정의·오토스케일에 전부 `count = var.enable_ecs ? 1 : 0`이
  붙어 있다. 08-09 오전에 이 줄을 "미적용"으로 적었는데 **틀린 서술이었다** — 낡은
  메모를 확인 없이 옮긴 것이다. 기본값으로 `terraform plan`을 돌려 `No changes`
  (exit 0)를 실측하고 고쳤다.
  게이팅이 없는 건 로그 그룹·시크릿·IAM 역할·ECR·SG·빈 클러스터인데 전부 $0~소액이고
  **일부러 남긴 것**이다(재구축 시 다시 채울 필요가 없다).
  > 교훈이 이 항목 자체보다 낫다 — **"안 한 일 목록"도 틀릴 수 있다.** 할 일을 적을 때
  > 상태를 재보지 않으면, 이미 한 일을 다시 하거나 안 한 일을 했다고 믿는다.
- [x] **AWS 네이티브 알림**(SNS + EC2 상태검사 알람) — ✅ 2026-08-09 (`terraform/alerts.tf`)
  - `StatusCheckFailed` 1분 × 2회 연속 → SNS. `watch.sh`(매시)가 못 줄이는
    "인스턴스가 통째로 죽음"을 몇 분으로 줄인다. **보는 층이 달라서 둘 다 남긴다.**
  - **`treat_missing_data = "notBreaching"`가 이 알람의 핵심.** 이 서버는 대부분 꺼져
    있고 꺼진 인스턴스는 지표를 안 낸다. 기본값이면 끌 때마다 INSUFFICIENT_DATA로
    떠서 '고장'과 '꺼둠'을 구분 못 한다 — watch.sh를 CloudWatch가 아니라 Actions로
    만든 이유와 같은 함정이다.
  - 자동 복구(`ec2:recover`)는 **일부러 안 붙였다.** 하드웨어 고장을 일부러 낼 수 없어
    "설정했다"에서 멈추기 때문이다. **재볼 수 있는 것만 켠다.**
  - `watch.sh`에 6-B를 추가해 **구독이 확인됐는지**를 매시 본다. SNS 이메일 구독은
    링크를 눌러야 살아나는데, 그전까지 알람은 정상적으로 울리고 정상적으로 아무에게도
    안 간다. 07-22에 `WARN`이 종료코드에 안 들어가 알림 0건이던 것과 같은 모양이다.

### 로드맵 밖에서 벌어진 것 (2026-08-03~08-09)

계획에 없던 일이 4주치 들어왔다. 나쁜 게 아니라 **범위가 늘어난 것**이니 기록해둔다(원칙 1).

- AI 초안 프롬프트 인젝션 가드 v2 (08-04 배포) — 진짜 공격은 금지어가 아니라 '전제 사슬'이었다
- 초대제 가입 + 데모 계정 폐지 (08-07) · 열린 가입은 영구히 안 연다(아래)
- **Web Push** (08-09 프로덕션) — 메일이 스팸함에 꽂히는 걸 도메인 없이 우회한 것
- 정적 개발일지 아카이브 + RSS + sitemap (08-09) — 서버가 꺼져 있어도 읽을 수 있다
- 개발일지 #25·#26 발행 → **연재 26/26, 미발행 0편**

---

## 학기 중 (2026-09-01~) ← **지금 여기다**

**기준: 30분씩 쪼개도 되는 것.**

✅ **(2026-09-05 확인) 이번 학기 가용 시간은 주당 5시간 이하다.**

그 값이 목록을 이렇게 바꾼다. **주당 5시간은 두 가지를 동시에 못 한다.** 개발일지 한 편이
초안·글투·docx·발행까지 2~3시간이고, SAA는 한 주에 3시간을 넣어도 두 달이 걸린다.
둘을 같이 밀면 둘 다 안 끝나므로 **자격증을 1순위로 두고 나머지는 유지 모드로 내린다.**

- 1순위 **SAA** — 주당 3시간을 여기 먼저 떼어놓는다. 나머지가 그 뒤를 쓴다.
- 2순위 **개발일지** — 편수 목표를 버린다. '무슨 일이 있으면 쓴다'로 바꾼다.
  이 프로젝트의 차별점이지만 42편이면 이미 충분히 증명됐고, 43편이 42편보다
  자격증 없는 상태를 덜 아프게 하지는 않는다.
- 3순위 **나머지 전부** — 아래 항목은 학기 중에 '하면 좋은 것'이지 계획이 아니다.
  주당 5시간에서 앞의 둘을 빼면 남는 게 거의 없다는 사실을 문서에 적어둔다.

⚠️ **시험·과제로 통째로 비는 주는 아직 안 적혀 있다.** 중간·기말 주간이 잡히면 그때
여기 적는다 — 그 주에 계획을 세워두면 못 지키고, 못 지킨 계획은 다음 계획도 못 믿게 만든다.

- [ ] SAA — **1순위**. 상태는 위 (1): 예약부터.
- [ ] 개발일지 — 유지 모드(무슨 일이 있을 때만). 이게 가장 큰 차별점이고 2년이면 50~80편
- [ ] **면접 문답 정리** — 일지에서 뽑아 Q&A 형태로. 원칙: **모든 답변에 날짜·수치·커밋 중 하나**
      (SAA가 끝난 뒤에 손댄다)
- [ ] **인턴 공고 보기** ← 지원 여부는 미정 (아래)
  - ✅ **(2026-09-05 확인) 아직 못 정했다.** 08-09의 "아직 안 정함"과 같은 값이라,
    이 줄은 한 달째 결정을 미루고 있다. **미정을 그대로 두면 시즌이 지나면서 자동으로
    '지원 안 함'이 된다** — 안 정한 게 아니라 정해지는 것이고, 그게 제일 나쁜 형태다.
    그래서 여기서는 이렇게 적어둔다: **기본값은 '공고만 본다'.** 주당 5시간(위 (2))에
    자소서·포트폴리오 정리를 넣을 자리가 없고, 서류 관문(자격증)도 아직 비어 있다.
    지원으로 바꾸려면 그 두 개 중 하나가 먼저 움직여야 한다.
    ☐ 마음이 바뀌면 이 줄을 고친다 — 다만 **공고를 한 번이라도 읽은 뒤에** 고친다.
  - 하계 인턴은 보통 봄 지원, 동계는 가을 지원. 개강 무렵이 동계 공고 시즌
  - **지원 안 해도 됨. 뭘 요구하는지만 봐도 충분** — 공고가 곧 로드맵이다
  - 1~2년 남았으면 인턴이 ROI 최고. 개인 프로젝트로는 못 얻는 것(남의 코드베이스,
    팀 단위 장애 대응, 실제 트래픽·비용)이 있고, 재학생만 지원 가능한 자리가 많다

---

## 나중에 / 지금은 안 함

떠오르는 건 여기 적고 넘어간다. **범위를 얼리지 않으면 영원히 안 끝난다.**

- 프론트 컴포넌트 테스트 (jsdom·testing-library 도입 필요)
- frontend deps PR (typescript-eslint가 TS 7 지원할 때까지 대기)
- 블로그에 이 프로젝트 외 주제의 글 쓰기
- ~~휴면 상태인 익명 Subscriber 테이블 완전 제거~~
  <ins>(**2026-09-02 해소** — 이미 지워졌다. `backend/alembic/versions/e7f8a9b0c1d2_drop_subscribers.py`가
  `op.drop_table("subscribers")`를 돈다. '나중에 / 지금은 안 함'에 남겨두면 이미 한 일을 다시
  하게 된다. 08-09에 `enable_ecs` 게이팅으로 같은 부류를 겪었고 그 교훈이 이 문서 안에 있다.)</ins>
- 실결제 전환 (사업자등록 필요 — 개인 포트폴리오로는 불가)

### 도메인 — **안 산다 (2026-08-07 결정)**

사면 세 가지가 한 번에 풀린다. 그래서 07-18·08-04에 두 번 저울에 올렸고, 08-07에
다시 올렸다가 **최종적으로 내려놨다.** 다시 꺼내지 말 것.

| 사면 풀리는 것 | 지금 상태 |
|---|---|
| 메일이 스팸함에 꽂히는 것 (DKIM·SPF 정렬) | Web Push로 우회했다. 알림은 푸시가 나른다 |
| SES 프로덕션 재신청 근거 | 08-04에 종결. 샌드박스로 계속 간다 |
| **오리진 평문 HTTP:8000** (커스텀 도메인 + ACM) | **안 풀린 채로 남는다** ← 유일한 실질 손해 |

**무료 도메인은 대안이 안 된다.** 판별 기준은 하나 — *CNAME 레코드를 넣을 수 있나*.
SES DKIM이 CNAME 3개, ACM 도메인 검증이 CNAME 1개를 요구한다.
- DuckDNS·No-IP 무료·js.org 류: 남의 도메인의 서브도메인이라 **CNAME을 못 넣는다** → 불가
- Freenom(.tk·.ml): 2023년 신규 등록 중단, 사실상 죽었다. 살아 있어도 수신 서버가
  그 TLD를 스팸 신호로 크게 잡아 DKIM을 붙여도 효과가 의심스럽다
- eu.org: NS를 위임해줘서 **기술적으로는 유일하게 가능.** 다만 수동 승인에 몇 주
- 그리고 포트폴리오 관점에선 `*.tk`·`*.duckdns.org`가 지금의 `*.cloudfront.net`보다 나쁘다

→ 남는 숙제는 **오리진 평문 HTTP 하나**다. 도메인 없이 줄이는 방법은 이미 넣어둔
오리진 공유 시크릿(엣지 우회 차단)이고, 그 이상은 도메인을 사기 전엔 못 한다.
**그 사실을 알고 안 사는 것**이지 잊은 게 아니다.

### 회원가입 재개방 — **영구히 없음 (2026-08-07 확정)**

08-04에 이미 기각했는데, 그때는 "SES 승인이 오면"이라는 조건이 이론상 남아 있었다.
**도메인을 안 사기로 하면서 그 조건이 영영 안 온다.** 이제 가정이 아니라 결론이다.

- `ALLOW_SIGNUP`은 프로덕션에서 **켜지지 않는다.** `.env`에 그 키 자체가 없는 게 정상
- 캡차·6자리 코드는 **만들지 않는다** (열린 가입을 전제한 계획이었다)
- 계정이 필요하면 **초대 링크**로 만든다(08-07 구현, `/admin`의 초대 섹션)
- `register` 라우트는 **지우지 않고 403으로 남긴다** — 404보다 안내가 낫고,
  "초대 링크가 있으면 그 링크로 가입하면 돼"라고 알려줄 자리가 필요하다

### 탄력적 IP(EIP) — **안 산다 (2026-08-28 결정)**

08-27 DR 게임데이가 "남은 개선 후보" 첫 줄에 올렸다. EIP가 있으면 재건해도 DNS가
안 바뀌고, 런북 8단계와 거기 있는 두 줄짜리 명령이 통째로 사라진다. 그래도 안 산다.

- **비용이 방향이 반대다.** 공인 IPv4는 붙어 있기만 하면 과금이라 EIP는 **인스턴스가
  꺼져 있는 동안에도** 시간당 $0.005, 월 $3.6쯤이 나간다. 이 서버는 대부분 꺼져 있으므로
  거의 전액이 노는 값이다. 08-28 실측 순지출이 $1.92/$10이라 감시 대상이 두 배 넘게 커진다.
- **고치려던 위험이 사실 그 자리에 없다.** 8단계가 위험했던 이유는 DNS가 바뀌어서가
  아니라 **긴 명령을 사람이 손으로 붙여넣다가 절반만 실행됐기 때문이다**(결함 D1,
  주차 해제가 주차로 뒤집혔다). 그건 "긴 명령은 파일에 적어 `bash`로 돌린다"로 막았고,
  EIP를 사도 그 부류의 다른 명령에는 그대로 남는다.
- **박힌 주소는 이미 없다.** 07-27 결함 F5 이후 스크립트 5개가 전부 태그로 조회한다.
  사람이 DNS를 옮겨 적는 자리는 런북 8단계 하나뿐이다.

**다시 꺼낼 조건은 하나뿐이다** — 서버를 상시 켜두는 운영으로 바꾸는 때. 그때는 실행 중
인스턴스의 공인 IPv4를 어차피 내고 있으므로 EIP의 추가 비용이 사실상 0이 된다.
그 전에는 **그 사실을 알고 안 사는 것**이지 잊은 게 아니다.

---

## 원칙

1. **범위를 늘리지 않는다.** 2026-07-20 하루에 커밋 21개가 나왔다. 좋은 작업이었지만
   그 페이스로 1~2년은 못 간다. 번아웃이 가장 큰 리스크.
2. **공고가 로드맵이다.** 뭘 할지 모르겠으면 가고 싶은 회사 공고 5개를 읽는다.
   반복해서 나오는 게 우선순위다.
3. **개발일지를 쓰면 그날은 끝.** 마침표를 찍었으면 더 벌이지 않는다.
4. **"설정했다"와 "동작한다"는 다르다.** 2026-07-20에 이걸 세 번 겪었다
   (백업 미실행 / 주석이 없는 방어를 있다고 서술 / 모델 교체 시 조용한 기본값 변화).
   뭘 만들든 산출물로 확인한다.
